package com.example.shelfplayer.core.designsystem.theme

import androidx.compose.material3.Typography

/**
 * PRODUCT_SPEC 2.10 / SET-002 — text scaling follows the system.
 *
 * Sizes are inherited from the Material 3 defaults, which are expressed in `sp` and therefore honour
 * the user's font-size setting. Overriding them with fixed `dp` values is the usual way an app
 * quietly breaks large-text accessibility, so this file deliberately customises nothing yet.
 */
internal val ShelfPlayerTypography: Typography = Typography()
