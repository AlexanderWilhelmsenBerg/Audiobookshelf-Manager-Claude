package com.example.shelfplayer.feature.settings

import androidx.compose.foundation.lazy.LazyListScope

/** Benchmark builds are release-like and expose no playback fault-injection UI. */
internal fun LazyListScope.playbackDiagnosticsRows() = Unit
