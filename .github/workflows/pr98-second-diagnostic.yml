name: PR 98 second failure diagnostic

on:
  push:
    branches:
      - diagnostic/pr98-second-failure

permissions:
  contents: write

jobs:
  diagnose:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0
      - uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: '17'
      - uses: gradle/actions/setup-gradle@v5
      - name: Capture app verification
        shell: bash
        run: |
          set +e
          ./gradlew :app:verifyDebug --continue -Pshelfplayer.warningsAsErrors=true --stacktrace > pr98-full.log 2>&1
          status=$?
          {
            echo "exit_status=$status"
            echo "--- last 800 lines ---"
            tail -n 800 pr98-full.log
          } > pr98-second-result.txt
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add pr98-second-result.txt
          git commit -m "Capture PR98 second failure diagnostic" || true
          git push
          exit 0
