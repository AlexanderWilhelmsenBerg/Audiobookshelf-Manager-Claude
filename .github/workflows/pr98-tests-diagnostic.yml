name: PR 98 tests diagnostic

on:
  push:
    branches:
      - diagnostic/pr98-tests

permissions:
  contents: write

jobs:
  diagnose:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
        with:
          ref: diagnostic/pr98-tests
          fetch-depth: 0
      - uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: '17'
      - uses: gradle/actions/setup-gradle@v5
      - name: Capture app unit tests
        shell: bash
        run: |
          set +e
          ./gradlew :app:testDebugUnitTest -Pshelfplayer.warningsAsErrors=true --stacktrace --console=plain > /tmp/pr98-tests.log 2>&1
          status=$?
          {
            echo "exit_status=$status"
            echo "--- last 500 lines ---"
            tail -n 500 /tmp/pr98-tests.log
          } > .github/pr98-tests-diagnostic.txt
          rm .github/workflows/pr98-tests-diagnostic.yml
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add .github/pr98-tests-diagnostic.txt .github/workflows/pr98-tests-diagnostic.yml
          git commit -m 'Capture PR98 unit-test diagnostic'
          git push origin HEAD:diagnostic/pr98-tests
