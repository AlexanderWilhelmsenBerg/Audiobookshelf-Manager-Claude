name: PR93 verify capture

on:
  push:
    branches:
      - diagnostic/pr93-verify-765
    paths:
      - .github/workflows/pr93-verify-capture.yml

permissions:
  contents: write

env:
  GRADLE_OPTS: -Dorg.gradle.jvmargs=-Xmx5g -Dorg.gradle.daemon=false

jobs:
  capture:
    runs-on: ubuntu-latest
    timeout-minutes: 60
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-java@v6
        with:
          distribution: temurin
          java-version: '17'
      - uses: gradle/actions/setup-gradle@v6
      - name: Capture verifyDebug output
        shell: bash
        run: |
          set +e
          ./gradlew verifyDebug --continue -Pshelfplayer.warningsAsErrors=true > verify-debug.txt 2>&1
          status=$?
          echo "exit_status=$status" >> verify-debug.txt
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add verify-debug.txt
          git commit -m "Capture PR93 verifyDebug failure" || true
          git push
          exit 0
