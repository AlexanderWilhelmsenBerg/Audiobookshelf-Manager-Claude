# Risk register

**As of:** 2026-08-21, opening Phase 6.

A companion to `docs/gaps.md`, and not the same document. A **gap** is a requirement the build does not
meet. A **risk** is something the build *does* — or does not know about itself — that could cost a user
their data, their playback, or this project its release.

Every entry below was verified against the tree on the date above, not inferred from a phase heading. Where
a claim is "never run", that means no artifact in this repository shows it running.

Each row carries the three things that make a risk actionable: what goes wrong, how bad it is if it does,
and the cheapest thing that would retire it. Ordered by blast radius within each group.

---

## 1. Release blockers

These stop a public build. None is a bug; each is a decision nobody has made.

| # | Risk | If it bites | Retired by |
| --- | --- | --- | --- |
| R-01 | **Closed 2026-08-21 — ADR-0024.** `applicationId` was `com.example.shelfplayer`, which Play rejects outright. | No release was possible, and changing it after anybody installed would produce a *second, empty* app — a fresh sign-in and every download lost. | `org.homebord.bookwave`, reverse-DNS of a domain the owner controls, moved before there is any release install to lose. The `applicationId` only: every `namespace` and Kotlin package stays `com.example.shelfplayer`, because Play neither sees nor cares about those and renaming them would touch the whole repository to change nothing observable. |
| R-02 | **Closed 2026-08-21 — ADR-0024.** `LICENSE` said "not yet chosen", so all rights were reserved and nothing could lawfully be distributed. | Distribution was unlawful rather than merely awkward. It also blocked the SBOM in PRODUCT_SPEC 18, which has to state a licence per component and for the work itself. | **GPL-3.0-or-later**, canonical text, with the project's notice and an explicit statement that BookWave is unaffiliated with the Audiobookshelf project. ADR-0012's posture — read that source for API facts, copy no code — is what made this a free choice rather than a forced one. ADR-0024 also records the GPL/Play interaction rather than leaving it to be found at upload time. |
| R-03 | **Closed 2026-08-21 — ADR-0024.** No distribution channel was chosen. | It decided the signing story, the version-code rule and whether a bundle or an APK ships, so several smaller decisions were blocked behind it. | **Google Play.** An App Bundle is the artefact, Play App Signing keeps key material out of the repository, a data-safety declaration is needed and `PRIVACY.md` is its source, and there is **no** reproducible-build requirement — which F-Droid would have imposed and which would have constrained the build in real ways. `versionCode` stays a hand-incremented integer, because Play's refusal to accept a reused code is permanent for a package and both a timestamp and a commit count can go backwards. |
| R-04 | **Closed 2026-08-20.** `versionName` had not moved in nine builds while the code advanced, so every field report in that window named the wrong build. | The debug console prints this string, so a user pasting diagnostics reported a version that did not identify their APK — the one job that field has. | The name moves with the code and states what the build is; it is at `0.9.13-switcher-unlock`, `versionCode` 39, and the convention plugin carries a comment saying why. |
| R-05 | **No signing configuration, deliberately** (PRODUCT_SPEC 15). Release builds from CI are unsigned. | Correct today. It becomes a risk the moment somebody needs a signed build in a hurry and puts a keystore where the repository can see it. | Key material in a protected environment with its own workflow, never in a push-triggered one. Write it down before it is needed. |
| R-06 | **Closed 2026-08-21 — ADR-0024.** No minimum server version was declared or enforced; `docs/api-compatibility.md` pinned 2.36.0 as the version *read*, not as a floor. | A user on an older server got failures the app described as its own. The capability probe softens this and cannot answer it: below 2.26.0 the server returns only the non-refreshable `user.token`, `POST /login` succeeds, everything appears to work, and AUTH-004's renewal fails hours later as an unexplained sign-out — an absence that shows up only at the first renewal, which no probe can catch. | **2.26.0**, in `ServerVersion.Minimum`, refused by `SignInViewModel` before a password is typed. The gate **fails open** on an unparseable version, deliberately the opposite of the profile lock, and `ServerVersionTest` asserts that direction. Residual, stated rather than discovered: a server between 2.26 and 2.36 is allowed and unverified. |

---

## 2. Things never verified on hardware

The largest cluster, and the one a device run keeps proving. Repeated device runs have found defects that
the whole JVM suite passed through — the audit of 2026-08-16 found eight, the run of 2026-08-20 found four,
and the signed-in route pass of 2026-08-23 found false grouped counts and an intercepted Genre Edit action.
One 27-case instrumented source set now exists and has passed once on hardware; every other module remains
JVM-only.

| # | Risk | If it bites | Retired by |
| --- | --- | --- | --- |
| R-07 | **Narrowed again 2026-08-23. The instrumented tier exists and its first hardware run passed; almost nothing is in it.** `:core:datastore` has `KeystoreLockCipherTest` and `ProfilePasscodeStoreTest` — 27 tests over the AndroidKeyStore wrap, staged write, and encrypted rate limit; all 27 passed on an API-36 Samsung. Every **other** module still has none, every automated UI assertion is Robolectric at `sdk = 34`, and connected tests never run in CI. | Robolectric asserts the semantics tree, which is what a screen reader consumes. It cannot assert what a real device does with it: no audio focus, real `MediaSession`, car, or route change. The observational signed-in screenshots found route defects but are not automation. Most of PRODUCT_SPEC 17.2 is still untested. | A managed-device runner plus playback/download/service/UI source sets. The working connected pattern is no longer the obstacle; coverage and CI hardware are. |
| R-08 | **The API matrix is mostly untested.** PRODUCT_SPEC 17.2 asks for API 26, 31, 34 and 36. Two files now run at more than one level, chosen because the level changes the mechanism rather than for coverage: `AppLocaleScreenTest` at 26, 33 and 34 — the per-app-language backport either side of `LocaleManager`'s arrival — and `WindowSizeScreenTest` at 26 and 34, where Lint warned that inset behaviour is target-SDK dependent. Everything else runs at 34. | API 26 is eight releases below anything else exercised. The storage-volume code remains written against documentation rather than a device, and no JVM run can tell you what a real API 26 device does with a media session. | Widening the Robolectric matrix further is cheap and has diminishing returns; the part that matters needs R-07. |
| R-09 | **The two-hour playback soak has never run** (PRODUCT_SPEC 17.3, 25). | The requirement is "no crash in 2-hour continuous playback". A leak in the session coordinator or the metrics recorder would appear at minute 90 and never in a unit test. | It needs a device and patience, not new infrastructure. Worth doing manually once and recording the result before any release. |
| R-10 | **The current Android Auto surface has never run in DHU/a head unit.** An older build passed discovery and media-button resume in a car on 2026-08-14, but the recent root, per-chapter completion badges, spoken-query path, current profile invalidation, and current artwork/actions are unexercised. Every one is a rendering/host contract — whether a head unit draws a completion bar from `EXTRAS_KEY_COMPLETION_PERCENTAGE` at all is up to the host, and phone screenshots cannot answer it. | A driver is the user least able to work around a broken screen. A tree that throws leaves the app listed and dead. | The DHU, which is free and runs on a workstation, followed by one physical head unit where practical. PRODUCT_SPEC 17.2 asks for it. |
| R-11 | **Process death and low storage are untested.** PRODUCT_SPEC 17.3 allows at most 10 s of progress loss after a forced kill; nothing measures it. | Product priority 2 is "do not lose progress", and its acceptance number has never been checked. | `adb shell am kill` plus a stopwatch, once, on a device. Cheap to do and impossible to claim without doing. |
| R-12 | **The release build is assembled but never executed.** `main.yml` runs `assembleRelease`, so R8 and resource shrinking run — and nothing installs or launches the result. | R8 breaks reflection-shaped code. Room, kotlinx.serialization and protobuf-lite have keep rules; Hilt, Media3, WorkManager and Coil rely on their libraries' own consumer rules. A missing rule is a release-only crash with a minified stack trace. | Launching the release APK once by hand, and ideally a smoke test on it in CI after R-07. |
| R-39 | **Closed on hardware 2026-08-23 for the ordinary Keystore/store half; open for key invalidation and biometric UI.** The 27 connected tests passed against a real AndroidKeyStore: round trip, IV variation, tamper rejection, deleted-key recovery, staged store behavior, and persistent rate limiting. | Permanent key invalidation/enrollment changes still fail closed and could lock out an owner; the system biometric window can still be broken despite correct policy code. | Exercise enrollment/key invalidation plus the biometric prompt with a person. The app-switcher thumbnail also remains unsuppressed on every level. |

---

## 3. Contracts taken from source rather than from a server

PRODUCT_SPEC 22.4 and 22.5 forbid guessing server behaviour and require a captured fixture before relying
on a shape. Three places rely on a shape read from the server's *source code* instead, which is a weaker
guarantee honestly labelled — and ADR-0012's posture makes reading it legitimate. This section exists so
nobody later mistakes a source-derived fixture for a captured one.

| # | Risk | If it bites | Retired by |
| --- | --- | --- | --- |
| R-13 | **Cover upload's contract is source-derived.** Multipart, part named `cover`, validated on the filename extension. Never captured, because it needs an account with the upload grant and an image the capture script should not invent. | An upload that fails on a real server fails at the one moment a user is watching, and the app cannot tell a rejected part name from a rejected image. | One capture against a server whose account holds `upload`. The demo account does not. |
| R-14 | **Embed metadata's contract is source-derived**, including the `task_finished` frame that carries the outcome. Starting one needs an administrator; the public demo account is refused. `TaskFramesTest`'s fixtures are hand-written from the 2.36.0 source and say so. | The feature rewrites the user's source audio files. A frame shape that differs means the app reports the wrong outcome for the most destructive operation it offers. | One capture from an admin account on a reachable server. Until then the confirmation wording carries the weight. |
| R-15 | **Closed, and the first version of this entry was wrong to call it unfixable.** Google Books answers `429` to GitHub Actions' address ranges, so the CI capture recorded an empty candidate list while the committed shape came from a real deployment — and the drift check compared whole directories, so the contract-capture job failed on **every run**. An empty key set is not an answer, so the capture script no longer writes one, and the compare step now works file by file: drift fails, a new target is a notice, and a target this run could not capture is skipped. | A permanently red check teaches people to ignore a red check, which is worse than no check. It had been red for at least the last three commits. | Done. The disagreement itself remains — the committed shape is from `audiobooks.dev` and CI cannot reproduce it — but it no longer masquerades as drift. |
| R-36 | **The socket fixtures recorded a race, and the drift check called it drift.** The server answers `auth` asynchronously, so a long-poll returns whatever has queued when it is answered: the `init` frame landed in `socket-auth.json` on one run and in `socket-event-after-progress.json` on the next — same frame, same keys, different file. Found only because fixing R-15 stopped the candidate-search fixture failing every run and masking it. | A byte-for-byte comparison cannot tell a race from the server changing its mind, so a green check would have been the *lucky* outcome rather than the correct one. | **Fixed on the second attempt.** The first fix polled until `init` appeared and *then* captured — but long-polling is destructive, so the probe drained the queue and the capture recorded an engine.io ping against nothing. The fixture went from carrying a race to carrying no frames at all. The poll that finds the frame has to be the poll that is recorded, so the capture itself now retries. **No fixture needed re-baselining.** Both committed files were always correct; it was the capture that was non-deterministic, and the retry makes it produce what they already said. The job is green. |
| R-16 | **A fixture reconstructed from a log is not a fixture.** `search-providers.json` was hand-transcribed from a CI log and was missing an entry; the drift check against a real capture caught it. | The lesson generalises: any fixture whose provenance is a log rather than a capture is a guess with a filename. | Already policy. Kept here because the failure was real and cost a debugging session. |

---

## 4. Data and account safety

| # | Risk | If it bites | Retired by |
| --- | --- | --- | --- |
| R-17 | **`accountType` defaults to `''` for any install that upgraded through migration 18**, until a sign-in or a permission refresh rewrites it — and `ProfileRole.ofAccountType("")` is `Listener`. | Nothing today: the UI gates on the `role` column, which sign-in writes. It stays a loaded gun for the next reader, because gating on `accountType` instead would silently demote every upgraded admin until their next refresh — the exact shape of the defect that hid the account-management row on a device. | **Named in a test rather than left in prose:** `version 18 leaves the account type empty, which is the least privileged role` asserts the default, the mapping, and that `role` is the column a permission check should read. A backfill on first launch would retire it entirely. |
| R-18 | **Closed.** Migrations 18 and 19 had no test of their own — 15, 16 and 17 always did, and the first version of this entry overstated it. The chain was covered either way, since every test migrates from its starting version all the way to current, but nothing asserted what 18 *defaults its new columns to*. Three tests now do, including one that pins R-17 by name. | A wrong default is the failure mode that survives a migration test: the step succeeds, the column exists, and its value is a lie. | Done. |
| R-19 | **Closed 2026-08-21.** The profile lock ships (AUTH-005, ADR-0023): six to twelve digits behind PBKDF2, the platform's biometric prompt from API 28, unlock tickets held only in memory so a cold start is locked, and `AutoStartDecision` answering ROUTE-002's clause — which this entry also named as blocked. | What this row described is now the user's choice rather than the build's: the lock is opt-in per profile, as §3.2 asks, so a profile that has declined a passcode is exposed exactly as before. That is the requirement rather than a residual risk. | Done, and it opens three narrower entries below — R-42, R-40 and R-41 — plus R-39 above. |
| R-42 | **Closed 2026-08-21.** The lock could lock an owner out of their own account: the curtain said signing in again clears the passcode, `ProfileLockRepository.forget` implemented that, and **no production code called it**. The curtain named two fallbacks and offered a control for neither. | Ten wrong attempts, or a record the Keystore could no longer unwrap after a lock-screen change, ended at a curtain with no exit. The only route left was Android's own "clear storage", which takes every download and every sign-in with it — product priority 2 reached from an unexpected direction, where the progress is not lost but unreachable, and the app did it deliberately. | `SignInUseCase` calls `LockedProfileRecovery.clearIfLocked`, and the curtain carries the password field that reaches it (`ReauthenticateField`), tested for both the exhausted and the unreadable record. The residual hazard — that this same call is silent when it fires from the ordinary sign-in screen — is R-44. |
| R-43 | **A unit test cannot notice that nothing calls the thing it tests.** `ProfileLockGate` had ten passing tests covering the relock arithmetic in full, while the feature was inert on a device: nothing in production called `onBackgrounded`, so `backgroundedAt` was never stamped, `isUnlocked` returned `true` for the life of the process, and all three relock options — `Immediately` included — behaved identically. The lock engaged on a cold start and never again. | Every green test was green about arithmetic that production never reached. This is R-37's shape a second time in one feature, which suggests it is the dominant way this codebase ships defects: not wrong logic, but correct logic that is unreachable, mis-wired, or fed by a double that does not resemble the real caller. | `ProcessLockWatcher` now wires it, and `ProcessLockWatcherTest` tests the *wiring* rather than the arithmetic — including the rotation case, where a naive counter would relock the app every time the phone turned sideways. The general mitigation is a habit rather than a test: after building a component, grep for its callers before believing it works. |
| R-44 | **Signing in again destroys a passcode without saying so.** `SignInUseCase` calls `clearIfLocked`, which is what makes the curtain's recovery route real and what lets a card's *Sign in again* rescue an exhausted or unreadable record. It fires from the ordinary sign-in screen too, where the user may only have meant to refresh an expired session — and nothing on that screen mentions the lock. The guard is `mayActivate`, which fails **closed**, so a transient disk error during sign-in also reaches `forget`. | A security setting the user chose disappears silently. Bounded in practice: the profile has to be locked at that moment, which for the *active* profile means the curtain is up and the only sign-in reachable is the curtain's own disclosed one. The undisclosed path is a non-active locked profile being re-signed-in from the switcher. Recoverable — set it again — and the user has just proved the account password, a strictly higher bar than six digits. | Reporting it: `clearIfLocked` returning whether it cleared, carried through `SignInOutcome` to a line on the screen the user lands on. Not done, because the sign-in screen navigates away on success and there is no obvious place for the sentence — which is a design question, not an oversight. |
| R-45 | **Closed 2026-08-21, and the fifth defect of R-43's shape in this one feature.** A locked profile that was *not* the active one could not be opened at all. The curtain reads `activeProfileId`, so it draws for one profile; `SwitchProfileUseCase` refuses a locked target before it becomes active. Tapping such a card produced "That account is locked. Enter its passcode to switch to it" and the app contained no field in which to enter it. | Both halves were individually correct and tested. A device with two accounts, the second carrying a passcode, would have shown the second one as permanently unopenable after any cold start — and the message told the user to do something that did not exist, which is the string-honesty failure this project has been most careful about elsewhere. | `ProfileSwitcherViewModel` asks `ProfileLockRepository.isLocked` before attempting the switch and opens a passcode dialogue. `isLocked` is the negation of the same `mayActivate` the use case calls, so prompt and refusal cannot disagree. Six tests in `ProfileSwitcherViewModelTest`, including that an already-unlocked protected profile is *not* prompted. |
| R-46 | **The vulnerability scan reports coordinates, not reachability.** `scripts/vulnerability-scan.sh` asks OSV about each of the SBOM's 175 components and fails the main-branch build on any advisory. It cannot tell whether the vulnerable code path is one this app ever executes. | Both directions cost something. A false alarm in a transitive library nothing calls blocks a release until somebody reasons about it; and there is **no exemption mechanism**, so the only ways past a finding today are to upgrade or to edit the script — the second of which is how a scan quietly stops scanning. | An exemption file with an expiry date and a stated reason per entry, designed the first time one is genuinely needed rather than now. As of 2026-08-21 there are **no advisories against any component**, which was confirmed to be a real result rather than a broken query by running the scan against an SBOM poisoned with `log4j-core:2.14.1` and watching it report all seven Log4Shell advisories. |
| R-47 | **Closed 2026-08-23. The active bearer could be sent to an origin an outside application chose.** `PlaybackService` is exported and has to be — Android Auto, Bluetooth and the platform media controls all reach the session through it. A Media3 controller may submit a pre-resolved item, `MediaItems.isReadyToPlay` was satisfied by any `localConfiguration` so a bare URI qualified, and the media streaming client is a clone of the authenticated one, whose `AuthorizationInterceptor` attached the ambient token to any request lacking a header with no check on destination. | Another installed application could name a host it controlled and have BookWave deliver the user's Audiobookshelf credential to it. Found by the 2026-08-22 review and confirmed in source before fixing. | **Two independent boundaries, either of which alone would stop it.** At the transport, `TokenProvider` now returns the token *with* the origin that issued it and the interceptor attaches it only on a scheme/host/port match, with anything unparseable resolving to withheld; the origin is a required argument on `activate`/`adopt`, so a future caller must supply one rather than inherit the last. At the playback layer, submitting a pre-resolved item is gated on `uid == Process.myUid()` — matched on UID because the platform assigns it and a package name is a claim arriving over IPC; external controllers keep the whole browse surface and submit app-issued ids only. Seven tests across two `MockWebServer` origins; removing the origin check turns two of them red. |
| R-48 | **Closed 2026-08-23. A connected car kept showing the previous profile's titles.** `onGetChildren` builds the browse tree on demand, which made it look self-updating. It is not: a browser fetches once and caches, and Media3 re-asks only after `notifyChildrenChanged` — which nothing called. | After a profile switch a head unit went on serving the account it had loaded first, so somebody else's book titles stayed on screen in a car with other people in it. Product priority 4 rather than stale UI, which is why the signal is the *profile* and not every library write. Finding 5 of the 2026-08-22 Android Auto review. | `AutoLibrary.invalidations()` emits on an active-profile change and `browsableParents()` owns the eight ids, so a tab added to `rootTabs` without being invalidated fails a test rather than a dashboard. `PlaybackService` collects it and notifies with the real child count. Six tests; removing the guard turns five red. **Delivery is not covered** — `notifyChildrenChanged` needs a real session and a connected browser, which is `:playback`'s absent instrumented tier and then the DHU. |
| R-40 | **The passcode is a six-digit secret behind a KDF, and that is all it is.** A million possibilities at 210,000 PBKDF2-HMAC-SHA256 iterations is roughly 2 × 10^11 hash iterations, which a single modern GPU exhausts in minutes. The verifier does not resist an attacker who is holding the file. | Nothing against the threat ADR-0023 actually names — somebody holding the unlocked phone, who has to go through the app and meets a rate limit stored inside the encrypted record where force-stopping cannot reset it. Everything, the moment this lock is described as protecting data at rest, or a later feature treats the verifier as if it were a key. | Nothing available, and that is the point of recording it. Raising the iteration count roughly triples a number that already loses, while making an old phone take seconds to reject a typo — and a lock that slow gets switched off. The mitigation is the wording: ADR-0023 and the curtain both say what this is, and neither may be quietly softened. |
| R-41 | **Closed 2026-08-21.** The relock delay fired for nobody: `ProfileLockGate` stamped every live ticket when the app left the foreground and compared that stamp to the clock on each read, and the two functions supplying the stamp had **no production caller**. `backgroundedAt` stayed null for the life of the process, so `isUnlocked` returned true unconditionally after one unlock and the setting's three values — `Immediately` included, which is the default — were indistinguishable. | A profile unlocked once stayed unlocked until Android killed the app, so the curtain guarded a cold start and nothing else. Somebody who turned the lock on expecting it to re-arm when they pocketed the phone had exactly the exposure R-19 used to describe, with a settings row telling them otherwise. | `ProcessLockWatcher` calls both functions from `Application.ActivityLifecycleCallbacks`, and `ProcessLockWatcherTest` fails if it is removed. It counts started activities and ignores `isChangingConfigurations`, so a rotation is not treated as leaving the app — without that, the default delay would demand a passcode every time the phone turned sideways. Whether `Immediately` is the right *default* is still open and does need a device. |
| R-20 | **`allowBackup="false"`**, so a device-to-device transfer carries nothing — not the sign-in, not the settings, not the downloads. | Deliberate, and the right default for a store of server credentials. It is a risk only in that a user replacing a phone will read it as data loss unless something says so. | A line in the release notes. Not a code change. |
| R-21 | **Progress is the only thing this app can lose that the user cannot recreate.** Everything else re-syncs. | Product priority 2. The outbox and the session coordinator exist for this, and R-11 is the reason the guarantee is unmeasured. | R-11. |

---

## 5. Playback correctness

| # | Risk | If it bites | Retired by |
| --- | --- | --- | --- |
| R-22 | **Excluded tracks resolve against the wrong offsets** (PLAY-003). A book whose server-side track list excludes a file maps player positions to book positions incorrectly. | A resume lands at the wrong point, in a book the user cannot tell is affected. Rare — nearly no book excludes a track — and silent when it happens. | `startOffset` carried into the media item's extras so `PlayerPositions` and `BookMediaSourceFactory` share one coordinate space, as ADR-0016 describes. Open since Phase 2. |
| R-23 | **A dropped websocket leaves an embed's outcome unknown.** Nothing replays a missed `task_finished`. | The app says "outcome unknown — verify on the server", which is honest and is also the worst possible answer about an operation that rewrote audio files. | Polling the task endpoint after a reconnect, if one exists. Not investigated. |
| R-24 | **Auto-play from a cold start is unreliable** — Android's background-start rules, not this app's. The setting says so. | A user who configured auto-play on a car connection sees it work sometimes. | Nothing. Correctly disclosed rather than fixed. |

---

## 6. Performance, never measured

| # | Risk | If it bites | Retired by |
| --- | --- | --- | --- |
| R-25 | **No baseline profile and no macrobenchmark module.** Three of PRODUCT_SPEC 17.3's four numbers — player start under 1 s, library interactive under 1 s, no ANR under download/playback stress — are unmeasured. The fourth was re-scoped by ADR-0025. | Startup and scroll are the two things a user judges in the first ten seconds. A baseline profile is typically worth 20–30% of cold-start time; the *consuming* half is already free, since `androidx.profileinstaller:1.4.0` is on the release classpath transitively, so shipping a `baseline-prof.txt` needs no new dependency. | A `:benchmark` module, on a device. **Partly retired:** `LargeLibrary` generates a seeded library of any size and `LargeLibraryScaleTest` runs the whole domain library surface over 2,000 books on every pull request — grouping, series, sorting, filtering, search and the shelves — asserting correctness and the shelf cap rather than a duration, because a JVM stopwatch on a CI runner measures the runner. What is left needs hardware: the four numbers, and a Room-or-server fixture at that size for the UI to be driven against. |
| R-26 | **Closed 2026-08-21 — ADR-0025.** The question was whether 17.3's *"scrolling grid ... on 2,000-item fixture library"* describes a screen this app has. **It does not.** A full sweep found `LazyVerticalGrid`, `LazyHorizontalGrid` and `GridCells` appearing zero times in the repository; Home is a `LazyColumn` of capped 20-item `LazyRow` shelves, the flat "all books" view is a list, and the library-browse destination was deliberately removed. | Building the benchmark first would have meant inventing a grid to satisfy a measurement, or measuring something else and calling it the target — the ADR-0016 failure again, where a "known defect" stood for four phases on one unchecked premise. | The target now measures `BooksView.List`, the screen that exists. ADR-0025 also adds the target 17.3 could not have named, because it describes a grid rather than an architecture: **there is no paging**, `Flow<List<Book>>` materialises every visible book per emission, and `HomeViewModel` reasons about that cost at 490 books — 2,000 is four times what the code was thought about at. Whether to adopt paging is deliberately left to the measurement. |
| R-27 | **Coil's memory cache is at its defaults** with covers loaded at shelf density. | Untested on a low-memory device, which is where an audiobook app spends its life — long sessions, screen off, background. | A measurement, not a change. Guessing at a cache size is how a cache gets worse. |

---

## 7. Accessibility and adaptive layout

| # | Risk | If it bites | Retired by |
| --- | --- | --- | --- |
| R-28 | **Narrowed: adaptive layouts exist, but wide hardware is not reviewed.** Book and player use two panes when width allows, sign-in is capped, and lists center rather than stretching. The 2026-08-23 review covered one portrait phone width only. | Tablet, foldable, split-screen, and landscape failures remain release-visible despite correct size-class branching. | One wide/tablet or resizable-window pass, one fold state, and landscape with Mini Player/insets visible. |
| R-29 | **Accessibility is enforced on the JVM; ordinary phone pixels were reviewed, TalkBack was not.** `assertEveryControlIsLabelled` and `assertEveryControlIsBigEnough` walk the semantics tree on Settings, book, downloads, profile switcher, player, mini player, and shelf, several at doubled font scale. The signed-in phone pass found a nested Genre Edit gesture failure, proving rendered reachability still matters. Label usefulness, contrast, TalkBack order, and 48dp platform dispatch remain out of reach. | TalkBack, large text, touch targets, and contrast are release requirements (§51, 2.10). The tree being correct is necessary and not sufficient. | One TalkBack/contrast/touch-target pass on a device and extending assertions to metadata plus every newly changed collection card. |
| R-35 | **The 48dp touch-target figure is asserted at 40dp.** `touchBoundsInRoot` — the rectangle the platform actually dispatches to, and the one that makes a Material `IconButton`'s 40dp state layer a 48dp target — is internal to Compose, and the public test API offers only equality assertions. So the check uses Material's smallest component size instead. | It still fails a 24dp icon used as a row's only control, which is the real failure mode. What it cannot prove is the last 8dp, which is a claim about Compose rather than about this app. | The device pass in R-29, or a public minimum-touch-target assertion in a future Compose release. |
| R-30 | **Large-text coverage is partial and RTL remains unexercised.** Several Robolectric screens render at doubled font scale, but the signed-in route inventory used the ordinary device scale and no RTL locale. | Long Norwegian labels at 200% and mirrored layouts can still clip or invert action order on routes outside the sampled tests. | Run the full changed-screen set at 2.0x and one RTL locale on a device or screenshot-test matrix. |

---

## 8. Process

| # | Risk | If it bites | Retired by |
| --- | --- | --- | --- |
| R-31 | **Local verification and CI can disagree.** Gradle considered test-compile tasks up to date when only the classpath had changed, so two stale test doubles compiled locally and failed in CI. | A green local build is not evidence. This cost a full debugging cycle once. | Already policy: verify with `--rerun-tasks`, and prefer a throwing property over an anonymous implementation of an interface that can grow. |
| R-32 | **Documentation drift has been the most frequent defect in this project.** The About tab described a build three phases old; `PRIVACY.md` and `SECURITY.md` still say "Phase 0: nothing leaves your device"; `docs/release.md` lists blockers that Phases 1–5 resolved. | Every one of these was user-visible or would have been read by somebody deciding whether to trust the app. | Treating user-facing prose as a deliverable of the change that invalidates it, not as a follow-up. |
| R-33 | **Twenty merged branches cannot be deleted from this session** (HTTP 403, and no MCP delete-branch tool). | Cosmetic. Recorded so it is not rediscovered. | A maintainer with push rights, or the GitHub UI. |
| R-34 | **Closed 2026-08-21.** There was no changelog and no label convention, both required by PRODUCT_SPEC 18 for a generated release note. | It blocked the release pipeline, cheaply. | `CHANGELOG.md` is written by hand and carries the reasoning behind each decision; `.github/release.yml` maps pull-request labels to sections so GitHub generates the note for a tag. The division is deliberate — the note indexes what merged, the changelog explains why. Unlabelled changes fall into a catch-all rather than being dropped, and nothing enforces a label, because a check that failed a pull request over one would block the fix for a labelling mistake. |
| R-37 | **A test double that does not reproduce the shape of the real thing hides defects behind a passing test.** PR #28 found that catalogue reconciliation deleted an entire library on any unchanged refresh: `LibrarySnapshot.books` carries only *expanded* items, an item the server reports unchanged is deliberately skipped, so a second refresh produced an empty list and called `markAllBooksDeleted` — and every read filters `isDeleted = 0`. A test named `refresh is idempotent and does not duplicate rows` had existed for months, refreshed twice, and passed. | It passed because `FakeAudiobookshelfGateway.listBooks` **ignored its `cached` argument** and returned every book on every call, so the fake's `books` was never empty and the production shape never occurred. The fake was not a double; it was a second implementation that agreed with nothing. A 60-second OkHttp `callTimeout` bounding the Media3 stream — PR #28's other serious find — escaped for the sibling reason: nothing in the suite plays for sixty-one seconds. | **The sync half is closed.** The fake now honours `cached.isUpToDate`, and the fixture books carry a server stamp — without one `isUpToDate` can never return true and the skip path is unreachable, which is why the blind spot existed. Reverting the production fix now fails `refresh is idempotent` through the full repository path. The playback half still needs R-07. |
| R-38 | **A defaulted parameter can preserve the bug it was added to fix.** `LibraryApi.listBooks` gained `onCatalogueBatch`, defaulting to `onBatch` so no implementer had to change. Any future persistence caller that does not pass a non-destructive sink silently gets the destructive behaviour the parameter exists to avoid. | Today there is exactly one persistence caller and it passes the sink. The trap is dormant, not absent, and it is the kind that reappears when a second caller is added by somebody reading the signature rather than the KDoc. | Removing the default once every caller is explicit, so the compiler asks the question instead of the reviewer. |

---

## What this register is not

It does not list requirements this build never claimed — those are in `docs/gaps.md`. It does not list
hypotheticals nobody has evidence for. And it does not list the things that look like risks and are not:
the empty capability set on a fresh server, the fake gateway's deliberate sign-in refusal, and the download
volume setting that moves no files are all in `gaps.md`'s final section with the reasoning.
