# Risk register

**As of:** 2026-08-21, opening Phase 6.

A companion to `docs/gaps.md`, and not the same document. A **gap** is a requirement the build does not
meet. A **risk** is something the build *does* — or does not know about itself — that could cost a user
their data, their playback, or this project its release.

Every entry below was verified against the tree on the date above, not inferred from a phase heading. Where
a claim is "never run", that means no artifact in this repository shows it running.

Each row carries the three things that make a risk actionable: what goes wrong, how bad it is if it does,
and the cheapest thing that would retire it. Ordered by blast radius within each group.

---

## 1. Release blockers

These stop a public build. None is a bug; each is a decision nobody has made.

| # | Risk | If it bites | Retired by |
| --- | --- | --- | --- |
| R-01 | **Closed 2026-08-21 — ADR-0024.** `applicationId` was `com.example.shelfplayer`, which Play rejects outright. | No release was possible, and changing it after anybody installed would produce a *second, empty* app — a fresh sign-in and every download lost. | `org.homebord.bookwave`, reverse-DNS of a domain the owner controls, moved before there is any release install to lose. The `applicationId` only: every `namespace` and Kotlin package stays `com.example.shelfplayer`, because Play neither sees nor cares about those and renaming them would touch the whole repository to change nothing observable. |
| R-02 | **Closed 2026-08-21 — ADR-0024.** `LICENSE` said "not yet chosen", so all rights were reserved and nothing could lawfully be distributed. | Distribution was unlawful rather than merely awkward. It also blocked the SBOM in PRODUCT_SPEC 18, which has to state a licence per component and for the work itself. | **GPL-3.0-or-later**, canonical text, with the project's notice and an explicit statement that BookWave is unaffiliated with the Audiobookshelf project. ADR-0012's posture — read that source for API facts, copy no code — is what made this a free choice rather than a forced one. ADR-0024 also records the GPL/Play interaction rather than leaving it to be found at upload time. |
| R-03 | **Closed 2026-08-21 — ADR-0024.** No distribution channel was chosen. | It decided the signing story, the version-code rule and whether a bundle or an APK ships, so several smaller decisions were blocked behind it. | **Google Play.** An App Bundle is the artefact, Play App Signing keeps key material out of the repository, a data-safety declaration is needed and `PRIVACY.md` is its source, and there is **no** reproducible-build requirement — which F-Droid would have imposed and which would have constrained the build in real ways. `versionCode` stays a hand-incremented integer, because Play's refusal to accept a reused code is permanent for a package and both a timestamp and a commit count can go backwards. |
| R-04 | **Closed 2026-08-20.** `versionName` had not moved in nine builds while the code advanced, so every field report in that window named the wrong build. | The debug console prints this string, so a user pasting diagnostics reported a version that did not identify their APK — the one job that field has. | The name moves with the code and states what the build is; it is at `0.9.13-switcher-unlock`, `versionCode` 39, and the convention plugin carries a comment saying why. |
| R-05 | ~~**No signing configuration, deliberately** (PRODUCT_SPEC 15). Release builds from CI are unsigned.~~ **Closed 2026-08-27.** | The prediction was that somebody needing a signed build in a hurry would put a keystore where the repository can see it. Rather than write that down and hope, the build now refuses it: `ReleaseSigning.kt` throws on a keystore inside the checkout, on a partly supplied set of inputs, and on a path that does not exist. The default with nothing supplied is unchanged — unsigned, and `main.yml` (push-triggered) supplies nothing. | Done. Key material comes from `~/.gradle/gradle.properties` or from secrets on the `workflow_dispatch`-only Build APK workflow; `docs/release.md` § Signing is the written-down part. |
| R-06 | **Closed 2026-08-21 — ADR-0024.** No minimum server version was declared or enforced; `docs/api-compatibility.md` pinned 2.36.0 as the version *read*, not as a floor. | A user on an older server got failures the app described as its own. The capability probe softens this and cannot answer it: below 2.26.0 the server returns only the non-refreshable `user.token`, `POST /login` succeeds, everything appears to work, and AUTH-004's renewal fails hours later as an unexplained sign-out — an absence that shows up only at the first renewal, which no probe can catch. | **2.26.0**, in `ServerVersion.Minimum`, refused by `SignInViewModel` before a password is typed. The gate **fails open** on an unparseable version, deliberately the opposite of the profile lock, and `ServerVersionTest` asserts that direction. Residual, stated rather than discovered: a server between 2.26 and 2.36 is allowed and unverified. |

---

## 2. Things never verified on hardware

The largest cluster, and the one a device run keeps proving. Repeated device runs have found defects that
the whole JVM suite passed through — the audit of 2026-08-16 found eight, the run of 2026-08-20 found four,
and the signed-in route pass of 2026-08-23 found false grouped counts and an intercepted Genre Edit action.
One 27-case instrumented source set now exists and has passed once on hardware; every other module remains
JVM-only.

| # | Risk | If it bites | Retired by |
| --- | --- | --- | --- |
| R-07 | **Narrowed again 2026-08-23. The instrumented tier exists and its first hardware run passed; almost nothing is in it.** `:core:datastore` has `KeystoreLockCipherTest` and `ProfilePasscodeStoreTest` — 27 tests over the AndroidKeyStore wrap, staged write, and encrypted rate limit; all 27 passed on an API-36 Samsung. Every **other** module still has none, every automated UI assertion is Robolectric at `sdk = 34`, and connected tests never run in CI. | Robolectric asserts the semantics tree, which is what a screen reader consumes. It cannot assert what a real device does with it: no audio focus, real `MediaSession`, car, or route change. The observational signed-in screenshots found route defects but are not automation. Most of PRODUCT_SPEC 17.2 is still untested. | A managed-device runner plus playback/download/service/UI source sets. The working connected pattern is no longer the obstacle; coverage and CI hardware are. |
| R-08 | **The API matrix is mostly untested.** PRODUCT_SPEC 17.2 asks for API 26, 31, 34 and 36. Two files now run at more than one level, chosen because the level changes the mechanism rather than for coverage: `AppLocaleScreenTest` at 26, 33 and 34 — the per-app-language backport either side of `LocaleManager`'s arrival — and `WindowSizeScreenTest` at 26 and 34, where Lint warned that inset behaviour is target-SDK dependent. Everything else runs at 34. | API 26 is eight releases below anything else exercised. The storage-volume code remains written against documentation rather than a device, and no JVM run can tell you what a real API 26 device does with a media session. | Widening the Robolectric matrix further is cheap and has diminishing returns; the part that matters needs R-07. |
| R-09 | **The two-hour playback soak has never run** (PRODUCT_SPEC 17.3, 25). | The requirement is "no crash in 2-hour continuous playback". A leak in the session coordinator or the metrics recorder would appear at minute 90 and never in a unit test. | It needs a device and patience, not new infrastructure. Worth doing manually once and recording the result before any release. |
| R-10 | **The current Android Auto surface has never run in DHU/a head unit.** An older build passed discovery and media-button resume in a car on 2026-08-14, but the recent root, per-chapter completion badges, spoken-query path, current profile invalidation, and current artwork/actions are unexercised. Every one is a rendering/host contract — whether a head unit draws a completion bar from `EXTRAS_KEY_COMPLETION_PERCENTAGE` at all is up to the host, and phone screenshots cannot answer it. | A driver is the user least able to work around a broken screen. A tree that throws leaves the app listed and dead. | The DHU, which is free and runs on a workstation, followed by one physical head unit where practical. PRODUCT_SPEC 17.2 asks for it. |
| R-11 | **Process death and low storage are untested.** PRODUCT_SPEC 17.3 allows at most 10 s of progress loss after a forced kill; nothing measures it. | Product priority 2 is "do not lose progress", and its acceptance number has never been checked. | `adb shell am kill` plus a stopwatch, once, on a device. Cheap to do and impossible to claim without doing. |
| R-12 | **The release build is assembled but never executed.** `main.yml` runs `assembleRelease`, so R8 and resource shrinking run — and nothing installs or launches the result. | R8 breaks reflection-shaped code. Room, kotlinx.serialization and protobuf-lite have keep rules; Hilt, Media3, WorkManager and Coil rely on their libraries' own consumer rules. A missing rule is a release-only crash with a minified stack trace. | Launching the release APK once by hand, and ideally a smoke test on it in CI after R-07. |
| R-39 | **Closed on hardware 2026-08-23 for the ordinary Keystore/store half; open for key invalidation and biometric UI.** The 27 connected tests passed against a real AndroidKeyStore: round trip, IV variation, tamper rejection, deleted-key recovery, staged store behavior, and persistent rate limiting. | Permanent key invalidation/enrollment changes still fail closed and could lock out an owner; the system biometric window can still be broken despite correct policy code. | Exercise enrollment/key invalidation plus the biometric prompt with a person. The app-switcher thumbnail also remains unsuppressed on every level. |

---

## 3. Contracts taken from source rather than from a server

PRODUCT_SPEC 22.4 and 22.5 forbid guessing server behaviour and require a captured fixture before relying
on a shape. Three places rely on a shape read from the server's *source code* instead, which is a weaker
guarantee honestly labelled — and ADR-0012's posture makes reading it legitimate. This section exists so
nobody later mistakes a source-derived fixture for a captured one.

| # | Risk | If it bites | Retired by |
| --- | --- | --- | --- |
| R-13 | **Cover upload's contract is source-derived.** Multipart, part named `cover`, validated on the filename extension. Never captured, because it needs an account with the upload grant and an image the capture script should not invent. | An upload that fails on a real server fails at the one moment a user is watching, and the app cannot tell a rejected part name from a rejected image. | One capture against a server whose account holds `upload`. The demo account does not. |
| R-14 | **Embed metadata's contract is source-derived**, including the `task_finished` frame that carries the outcome. Starting one needs an administrator; the public demo account is refused. `TaskFramesTest`'s fixtures are hand-written from the 2.36.0 source and say so. | The feature rewrites the user's source audio files. A frame shape that differs means the app reports the wrong outcome for the most destructive operation it offers. | One capture from an admin account on a reachable server. Until then the confirmation wording carries the weight. |
| R-15 | **Closed, and the first version of this entry was wrong to call it unfixable.** Google Books answers `429` to GitHub Actions' address ranges, so the CI capture recorded an empty candidate list while the committed shape came from a real deployment — and the drift check compared whole directories, so the contract-capture job failed on **every run**. An empty key set is not an answer, so the capture script no longer writes one, and the compare step now works file by file: drift fails, a new target is a notice, and a target this run could not capture is skipped. | A permanently red check teaches people to ignore a red check, which is worse than no check. It had been red for at least the last three commits. | Done. The disagreement itself remains — the committed shape is from `audiobooks.dev` and CI cannot reproduce it — but it no longer masquerades as drift. |
| R-36 | **The socket fixtures recorded a race, and the drift check called it drift.** The server answers `auth` asynchronously, so a long-poll returns whatever has queued when it is answered: the `init` frame landed in `socket-auth.json` on one run and in `socket-event-after-progress.json` on the next — same frame, same keys, different file. Found only because fixing R-15 stopped the candidate-search fixture failing every run and masking it. | A byte-for-byte comparison cannot tell a race from the server changing its mind, so a green check would have been the *lucky* outcome rather than the correct one. | **Fixed on the second attempt.** The first fix polled until `init` appeared and *then* captured — but long-polling is destructive, so the probe drained the queue and the capture recorded an engine.io ping against nothing. The fixture went from carrying a race to carrying no frames at all. The poll that finds the frame has to be the poll that is recorded, so the capture itself now retries. **No fixture needed re-baselining.** Both committed files were always correct; it was the capture that was non-deterministic, and the retry makes it produce what they already said. The job is green. |
| R-16 | **A fixture reconstructed from a log is not a fixture.** `search-providers.json` was hand-transcribed from a CI log and was missing an entry; the drift check against a real capture caught it. | The lesson generalises: any fixture whose provenance is a log rather than a capture is a guess with a filename. | Already policy. Kept here because the failure was real and cost a debugging session. |

---

## 4. Data and account safety

| # | Risk | If it bites | Retired by |
| --- | --- | --- | --- |
| R-17 | **`accountType` defaults to `''` for any install that upgraded through migration 18**, until a sign-in or a permission refresh rewrites it — and `ProfileRole.ofAccountType("")` is `Listener`. | Nothing today: the UI gates on the `role` column, which sign-in writes. It stays a loaded gun for the next reader, because gating on `accountType` instead would silently demote every upgraded admin until their next refresh — the exact shape of the defect that hid the account-management row on a device. | **Named in a test rather than left in prose:** `version 18 leaves the account type empty, which is the least privileged role` asserts the default, the mapping, and that `role` is the column a permission check should read. A backfill on first launch would retire it entirely. |
| R-18 | **Closed.** Migrations 18 and 19 had no test of their own — 15, 16 and 17 always did, and the first version of this entry overstated it. The chain was covered either way, since every test migrates from its starting version all the way to current, but nothing asserted what 18 *defaults its new columns to*. Three tests now do, including one that pins R-17 by name. | A wrong default is the failure mode that survives a migration test: the step succeeds, the column exists, and its value is a lie. | Done. |
| R-19 | **Closed 2026-08-21.** The profile lock ships (AUTH-005, ADR-0023): six to twelve digits behind PBKDF2, the platform's biometric prompt from API 28, unlock tickets held only in memory so a cold start is locked, and `AutoStartDecision` answering ROUTE-002's clause — which this entry also named as blocked. | What this row described is now the user's choice rather than the build's: the lock is opt-in per profile, as §3.2 asks, so a profile that has declined a passcode is exposed exactly as before. That is the requirement rather than a residual risk. | Done, and it opens three narrower entries below — R-42, R-40 and R-41 — plus R-39 above. |
| R-42 | **Closed 2026-08-21.** The lock could lock an owner out of their own account: the curtain said signing in again clears the passcode, `ProfileLockRepository.forget` implemented that, and **no production code called it**. The curtain named two fallbacks and offered a control for neither. | Ten wrong attempts, or a record the Keystore could no longer unwrap after a lock-screen change, ended at a curtain with no exit. The only route left was Android's own "clear storage", which takes every download and every sign-in with it — product priority 2 reached from an unexpected direction, where the progress is not lost but unreachable, and the app did it deliberately. | `SignInUseCase` calls `LockedProfileRecovery.clearIfLocked`, and the curtain carries the password field that reaches it (`ReauthenticateField`), tested for both the exhausted and the unreadable record. The residual hazard — that this same call is silent when it fires from the ordinary sign-in screen — is R-44. |
| R-43 | **A unit test cannot notice that nothing calls the thing it tests.** `ProfileLockGate` had ten passing tests covering the relock arithmetic in full, while the feature was inert on a device: nothing in production called `onBackgrounded`, so `backgroundedAt` was never stamped, `isUnlocked` returned `true` for the life of the process, and all three relock options — `Immediately` included — behaved identically. The lock engaged on a cold start and never again. | Every green test was green about arithmetic that production never reached. This is R-37's shape a second time in one feature, which suggests it is the dominant way this codebase ships defects: not wrong logic, but correct logic that is unreachable, mis-wired, or fed by a double that does not resemble the real caller. | `ProcessLockWatcher` now wires it, and `ProcessLockWatcherTest` tests the *wiring* rather than the arithmetic — including the rotation case, where a naive counter would relock the app every time the phone turned sideways. The general mitigation is a habit rather than a test: after building a component, grep for its callers before believing it works. |
| R-44 | **Closed 2026-08-24 — ADR-0026 decision 2.** Signing in again could destroy a profile passcode with nothing on screen mentioning it. `SignInUseCase` called `clearIfLocked` on every successful sign-in, including from the ordinary sign-in screen where a user may only have meant to refresh an expired session. | `clearIfLocked` guards on `mayActivate`, so it was not *always* destructive — but `mayActivate` fails **closed**, so it cleared in exactly the two cases that matter: a routine re-authentication while the profile was in its locked state, and a transient failure reading the lock record. The second is the sharper edge — a disk error made the app *more* permissive, in the one place whose job is to be less so. | `invoke` now takes a `SignInIntent` with **no default**, so the compiler asks each of the two call sites which it is (R-38: a defaulted parameter preserves the bug it was added to fix). Only the curtain passes `RecoverLockedProfile`, and the curtain is the only screen that says what will happen first — `lock_forgot_signin`, and `lock_exhausted` after ten wrong attempts. Reverting the check turns `ordinary reauthentication preserves the passcode` red. |
| R-45 | **Closed 2026-08-21, and the fifth defect of R-43's shape in this one feature.** A locked profile that was *not* the active one could not be opened at all. The curtain reads `activeProfileId`, so it draws for one profile; `SwitchProfileUseCase` refuses a locked target before it becomes active. Tapping such a card produced "That account is locked. Enter its passcode to switch to it" and the app contained no field in which to enter it. | Both halves were individually correct and tested. A device with two accounts, the second carrying a passcode, would have shown the second one as permanently unopenable after any cold start — and the message told the user to do something that did not exist, which is the string-honesty failure this project has been most careful about elsewhere. | `ProfileSwitcherViewModel` asks `ProfileLockRepository.isLocked` before attempting the switch and opens a passcode dialogue. `isLocked` is the negation of the same `mayActivate` the use case calls, so prompt and refusal cannot disagree. Six tests in `ProfileSwitcherViewModelTest`, including that an already-unlocked protected profile is *not* prompted. |
| R-46 | **The vulnerability scan reports coordinates, not reachability.** `scripts/vulnerability-scan.sh` asks OSV about each of the SBOM's 175 components and fails the main-branch build on any advisory. It cannot tell whether the vulnerable code path is one this app ever executes. | Both directions cost something. A false alarm in a transitive library nothing calls blocks a release until somebody reasons about it; and there is **no exemption mechanism**, so the only ways past a finding today are to upgrade or to edit the script — the second of which is how a scan quietly stops scanning. | An exemption file with an expiry date and a stated reason per entry, designed the first time one is genuinely needed rather than now. As of 2026-08-21 there are **no advisories against any component**, which was confirmed to be a real result rather than a broken query by running the scan against an SBOM poisoned with `log4j-core:2.14.1` and watching it report all seven Log4Shell advisories. |
| R-47 | **Closed 2026-08-23. The active bearer could be sent to an origin an outside application chose.** `PlaybackService` is exported and has to be — Android Auto, Bluetooth and the platform media controls all reach the session through it. A Media3 controller may submit a pre-resolved item, `MediaItems.isReadyToPlay` was satisfied by any `localConfiguration` so a bare URI qualified, and the media streaming client is a clone of the authenticated one, whose `AuthorizationInterceptor` attached the ambient token to any request lacking a header with no check on destination. | Another installed application could name a host it controlled and have BookWave deliver the user's Audiobookshelf credential to it. Found by the 2026-08-22 review and confirmed in source before fixing. | **Two independent boundaries, either of which alone would stop it.** At the transport, `TokenProvider` now returns the token *with* the origin that issued it and the interceptor attaches it only on a scheme/host/port match, with anything unparseable resolving to withheld; the origin is a required argument on `activate`/`adopt`, so a future caller must supply one rather than inherit the last. At the playback layer, submitting a pre-resolved item is gated on `uid == Process.myUid()` — matched on UID because the platform assigns it and a package name is a claim arriving over IPC; external controllers keep the whole browse surface and submit app-issued ids only. Seven tests across two `MockWebServer` origins; removing the origin check turns two of them red. |
| R-48 | **Closed 2026-08-23. A connected car kept showing the previous profile's titles.** `onGetChildren` builds the browse tree on demand, which made it look self-updating. It is not: a browser fetches once and caches, and Media3 re-asks only after `notifyChildrenChanged` — which nothing called. | After a profile switch a head unit went on serving the account it had loaded first, so somebody else's book titles stayed on screen in a car with other people in it. Product priority 4 rather than stale UI, which is why the signal is the *profile* and not every library write. Finding 5 of the 2026-08-22 Android Auto review. | `AutoLibrary.invalidations()` emits on an active-profile change and `browsableParents()` owns the eight ids, so a tab added to `rootTabs` without being invalidated fails a test rather than a dashboard. `PlaybackService` collects it and notifies with the real child count. Six tests; removing the guard turns five red. **Delivery is not covered** — `notifyChildrenChanged` needs a real session and a connected browser, which is `:playback`'s absent instrumented tier and then the DHU. |
| R-49 | **Closed 2026-08-23. A profile switch was not a transaction, so one listener's position could land on the next one's row.** `SwitchProfileUseCase` had no playback collaborator at all: it wrote the new selection while the previous account's book kept playing and kept journaling. Every write resolved `activeProfileId()` at the moment it reached Room, and `PlaybackService.flushProgress` deliberately launches on the application scope so it survives service death — which is also what let it survive a profile change. | PRODUCT_SPEC 6.5 states the order — flush, pause, then change context — and none of it happened. The five-second journal against a microsecond switch is a race somebody hits by switching accounts while a book plays, which is the ordinary way two people share a phone. Product priority 4, and priority 2 from the other side: the outgoing listener's position is not merely misfiled, it is *gone* from their own row. P0 of the 2026-08-22 review. | **Two independent halves, because ordering alone does not close it.** `PlaybackHandover` — a `:domain` seam bound in `:app` over `PlaybackController`, following `StartupPlayer` — pauses, flushes and clears the queue, **awaited**, before `setActiveProfile`. And every playback write now carries an explicit `ProfileId`, read from the loaded book's own extras (`MediaItems.KEY_OWNER_PROFILE_ID`, stamped from `PlaybackSession.profileId`) at the moment the position is read rather than resolved at the far end of a launch — so a tick already in flight when the switch begins still lands on the right row. A write for a departed account is stored but raises no smart download, since `SmartDownloadUseCase` fetches for whoever is active. Nine tests across three suites; reverting the ordering turns two red, reverting the owner two more, removing the extras stamp one. |
| R-50 | **Closed 2026-08-23. The bookmark write resolved the active profile inside the write.** `DefaultBookmarkRepository.withScope` asked `activeProfileId()` exactly as the progress journal used to, and its comment said that was right because "there is no caller that could sensibly name a different profile". `PlaybackService.bookmarkHere` is that caller: a car's custom command, launched on the application scope so a bookmark dropped as the car disconnects still lands — which is precisely a write that can finish after a profile switch. | Narrower than what R-49 closed, and left open deliberately at the time rather than missed: it needs a bookmark press and a switch in the same instant, two simultaneous human actions rather than a timer against one, and R-49's handover clears the player's queue so `bookmarkHere` finds no item afterwards. The residual window was the microseconds *during* the handover. The damage was also smaller — a bookmark on a stranger's account is visible and removable, where a position silently overwrote a progress bar. | `BookmarkRepository.add` takes the same optional `ProfileId` the position and history writes take, read from `MediaItems.ownerOf` on the main thread with the position rather than at the far end of the launch. `rename` and `remove` deliberately do **not**: they are driven from a sheet by somebody looking at the bookmark they are editing, and the profile cannot change under them without the same person also opening the switcher. Two tests; reverting the lookup turns one red. |
| R-51 | **Closed 2026-08-23. Three writes shipped against a server nobody had watched answer them.** Cover upload, metadata embedding and user activation had no captured contract, and there was no adapter-level `AbsManagementContractTest` at all — every sibling adapter had one. The shapes were written from the requirements and from the routes somebody had already seen. | Two of the four findings would have been guessed wrong. The upload answers **JSON** where every other write on this API answers `text/plain OK`, and the duplicate-embed `400` answers **`text/html`** where `ManagementService`'s own KDoc asserted `text/plain` "like every other `sendStatus` route". A third is security-relevant: `PATCH /api/users/{id}` returns the updated user **with a live `token`**, the same hazard already recorded for `GET /api/users`. P0 of the 2026-08-22 review. | `scripts/capture-contracts.sh` drives all three against a throwaway 2.36.0 container — the same version the owner's server runs — and records eight new fixtures: three permitted, three refused, the duplicate embed, and the reactivation. `AbsManagementContractTest` replays them through the real adapter: ten tests covering path, method, bearer, the multipart part's field name, `backup=1` on the query string, the exact one-key PATCH body, the absent re-read after a refusal, and that the user response is never parsed so its token cannot reach the app. The embed capture is safe only because it runs against eight seconds of generated silence in a container that is deleted afterwards; it rewrites real audio files and must never be pointed at a library anybody owns. |
| R-52 | **Closed 2026-08-23. The capture committed the client's IP address, and reported false drift because of it.** A play session records `ipAddress`, and the scrubber's volatile-key list did not include it. | Two problems from one omission. A GitHub runner's Docker bridge reports `::ffff:172.17.0.1` and a local one reports `172.17.0.1`, so re-capturing the *identical* server version showed drift on `item-play.json` and `multi-item-play.json` and nothing else — the false positive R-15's note already warns trains a reader to ignore a red check. And it is the one field in the whole capture that would carry a real address if the script were ever pointed at a server outside a throwaway container, which PRODUCT_SPEC 14.5 forbids committing. | `ipAddress` joins `lastScan`, `startedAt` and `date` in `VOLATILE_KEYS`, with the reasoning recorded beside them. With it in place, a fresh capture of 2.36.0 is byte-identical to the committed fixtures across all 56 pre-existing files — which is the first time the drift check has actually been able to report that. |
| R-53 | **Closed 2026-08-23. The socket capture threw away a frame the server had sent, and the drift check went red on a pull request that had not touched a socket.** The auth poll retried until `init` appeared and **overwrote** its fixture on each attempt. `user_online` is broadcast when the `40` CONNECT lands and `init` answers the `auth` posted after it, so on a slow runner the first poll drained `user_online` alone and the second replaced it with `init`. | The script's own comment had predicted this exact split and called it a residual — *"nothing guarantees it"* — so the failure mode was known, documented and live for however long it took to fire. It cost a red **Capture Audiobookshelf contracts** on PR #35, whose entire `core/network` diff was eight lines adding a `ProfileId` to a mapper. That is the tax R-15 describes: a check nobody can act on, on a change that did not cause it, asked about before every merge. | The polls now **accumulate** instead of overwriting, and engine.io keepalives are dropped so the fixture cannot depend on how many polls the race took. Safe because long-polling is destructive — each frame reaches exactly one poll, so appending cannot double-count — and the two frames have a fixed causal order, so the assembled sequence is the same whether they arrive together or apart. Verified two ways: the accumulator produces an identical frame sequence for one-poll, split, split-with-keepalive and split-with-empty-poll inputs; and a live capture against a fresh 2.36.0 container is byte-identical to all 64 committed fixtures. |
| R-54 | **Closed 2026-08-23. A setting the user could see, change and store, that nothing read.** `WorkManagerDownloadScheduler.enqueue` asked `policy.allowsCellular(TrafficCategory.ManualDownload)` whoever had called it, so a smart download inherited the manual rule. `NetworkPolicy` keeps the two apart deliberately and says why — *"a manual download is a decision the user just made and a smart one is the app deciding for them"* — and `DownloadBookUseCase` already carried the `isAutomatic` flag that distinguishes them, for the paused-book guard. It simply never reached the scheduler. | A listener who allowed cellular for downloads but not for smart downloads got smart downloads on cellular anyway: the app spending their data unasked, which is the one thing the separation exists to prevent. It went unnoticed because **both defaults are `false`** — the two settings agreed until somebody turned one on, so every test and every device run to date exercised a configuration in which the bug is invisible. This is the sixth instance of R-37/R-43's shape in this codebase: not wrong logic, but correct logic nothing reaches. | `DownloadScheduler.enqueue` takes a `TrafficCategory`; the use case maps its own `isAutomatic` to one in a single place, so there is no second mapping to fall out of step and a fourth category would make the compiler ask. **And the fix had to close a second hole it would otherwise have opened.** `enqueueUniqueWork` used `ExistingWorkPolicy.KEEP`, which lost nothing while every job carried the same constraint; once the two can differ, a sweep's `UNMETERED` job would survive the listener's later tap and their explicit override would do nothing. A manual request now replaces existing work in exactly the configuration where it may relax a constraint, and never otherwise — and a sweep never replaces a tap, because the listener's permission is the more permissive of the two. Replacing costs no progress: the worker resumes from the `.part` files. Twelve tests. Reverting the category turns three red, reverting the replace rule one, reverting the use-case mapping two. |
| R-55 | **Closed 2026-08-23. Two controls decided when a car starts playing, and the one without a warning won.** ROUTE-002's per-device policies shipped in Phase 4 — `AutoStartDecision` with a full truth table, `OutputDeviceWatcher` reading each device's own answer. `PlaybackService.onPostConnect` went on reading a global `autoPlayOnCarConnect` boolean from a different Settings section, and its own comment still claimed the per-device policies *"are not built"*. | A listener who had set their car to **Never react** in the device list could still be auto-played by a switch on another screen. The device list carries a warning about background restrictions and defaults to `Arm only`; the global switch carried neither, so the warning described something other than what happened. Both paths did check the profile lock, so AUTH-005 held throughout. Finding 2 of the 2026-08-22 Android Auto review, and the seventh instance of R-37/R-43's shape: correct logic behind a caller that never reached it. | The trigger stays and only the policy moves — deleting it would have been a regression, because `OutputDeviceWatcher` listens to `AudioDeviceCallback` and a projected Android Auto or Automotive OS connection arrives as a *media controller*, which `DeviceKind.Car`'s own KDoc already said. `CarConnection.decide` holds the three lines (remember, `policyFor(CAR_ID)`, decide) so a test can reach the **wiring** rather than only the table, which is the lesson R-43 recorded. The global switch is removed from the model, the repository, the datastore writer and Settings; the proto field stays so its number is never reused, and its stored bit seeds the car's policy as `AutoPlay` for anyone who had turned it on. Fourteen tests. Reverting the policy lookup turns four red, reverting the seed's stored-policy guard two. |
| R-56 | **A green `verifyDebug` in a long session is not always evidence.** On 2026-08-23 `:app:kspDebugKotlin` failed with `'PlaybackHandover' could not be resolved` on a tree that was correct — the class was in `:domain`'s jar with that build's timestamp and the import was intact — then passed on the third identical run. The cause was memory: accumulated daemons held 6.3 GB, 3.3 GB and 1.2 GB, leaving 4.7 GB, and an incremental KSP round could not resolve symbols. After `./gradlew --stop` the same tree had 15.1 GB free and gated green in 10m42s, and CI agreed. | The failure reads exactly like a missing type, so the first instinct is to hunt a defect that is not there — an hour was spent doing that. The worse direction is the other one: the same pressure could make a run pass by luck, and `CLAUDE.md` tells every session "never report work complete while `verifyDebug` fails" without saying that a failure can be environmental. R-31 already recommends `--rerun-tasks` for a classpath change, which makes the build longer and the pressure higher. | **Run `./gradlew --stop` before the gate on a long session, and check `free -m` before believing a KSP resolution error.** Two independent verdicts settle it: a local gate with a fresh daemon, and CI, which runs on a clean runner and is the authority. The build cache masks this in both directions — a cached task reports success without executing — so `--no-build-cache` is what makes a local gate mean anything after a suspicious failure. Not fixed in the build itself: lowering `org.gradle.jvmargs` would slow every build for a condition that only appears in hour-long sessions. |
| R-57 | **Closed 2026-08-23. A contract fixture recorded a poll window, and a poll window is not reproducible.** `socket-embed-task.json` committed everything the embed's socket returned. On the runner that window also held the socket's own `init` frame — the handshake's, which the two drain polls had not yet been given — and a `task_progress` and a `track_progress`, none of which a local capture of the same 2.36.0 had seen. The drift check went red on a pull request whose only socket change was adding this file, and the diff was purely additive: the four lifecycle frames matched byte for byte. | It is R-53's failure inverted — there a frame the server sent was lost, here frames it sent arrived somewhere the fixture did not expect — and it has R-53's cost, which is a red check that means nothing and teaches a reader to skip the next one. | The capture reduces the window to the first `task_started`, `track_started`, `track_finished` and `task_finished`, in that fixed order: the four an embed produces exactly once and the four `TaskFrames` reads. Proven by replaying the runner's frame set through the filter and `record` — it reproduces the committed fixture byte for byte, and without the filter it does not. The progress frames are written into `docs/api-compatibility.md` from the CI log, observed but not committed as a contract. |
| R-65 | **Closed 2026-08-27. The second half of the same lesson as R-57: `item-embed-metadata-repeated` captured a race, not a contract.** The fixture pins the documented refusal for a second embed request — `400`, *"Library item is already in queue or processing"* — and the capture sent it "with no delay on purpose — the first task is still running". Embedding an eight-second file takes about 50 ms and the request needs about 45 ms to arrive, so which side of the finish it lands on is a coin flip. The 2026-08-27 run on PR #52 lost it by 4 ms, got `200 OK`, and the drift check reported it as a changed contract. | Identical in kind to R-57 and R-15: a red check that means nothing, on a pull request that had not touched the embed capture at all. The server had not changed its mind about anything — the capture had raced itself. Left alone it would fail roughly whenever the runner happened to be fast. | The capture retries, up to six attempts, and **a `200` is not a failed attempt**: it queues a task of its own, so each attempt creates the condition the next one tests for. If every attempt still drains, the run writes nothing and the compare step reports the fixture as not-captured — the same rule the candidate search follows, so the committed `400` survives and a genuine change in the refusal is still caught. Both branches were exercised against a real 2.36.0: the ordinary path captured the `400` on attempt 1 byte-identical to what is committed, and a harness that forces every attempt to drain produced 65 fixtures, no drift, and the committed refusal untouched. |
| R-59 | **Closed 2026-08-24. Any installed application could enumerate a private library through the exported media session.** `MediaLibraryService` is `exported="true"` and must be — un-exporting it removes the app from Android Auto and Assistant — and `onConnect` granted `DEFAULT_SESSION_AND_LIBRARY_COMMANDS` plus four custom commands to whatever bound to it. The five browse callbacks answered every caller with the active profile's book titles, and `ADD_BOOKMARK` let any of them write into the library. | PRODUCT_SPEC 14.5 is about exactly this data. The pre-resolved item path was already UID-gated, so no outside app could ever inject a URI into the authenticated client; what was open was reading, searching and one write. | ADR-0026. `ControllerTrust.accessFor` splits callers into `LibraryAndPlayback` and `PlaybackOnly` using Media3's own predicates — `isTrusted` (the platform's `isTrustedForMediaControl`), `isAutomotiveController`, `isAutoCompanionController`, `isMediaNotificationController` — plus the service's own UID, and never the caller's claimed package name. Untrusted callers keep `DEFAULT_SESSION_COMMANDS`, so a headset, a watch and a Bluetooth remote still work. Two mechanisms: the command set is the gate Media3 enforces, and the five callbacks refuse again for the legacy browser path. Both mutations — a package allowlist, and the original grant-everyone — turn four tests red. |
| R-60 | **The controller-trust adapter is not reachable from a JVM test.** `ControllerTrust.accessFor` is a pure function with ten tests, but the six facts it consumes are read by `MediaSession.identityOf`, and `MediaSession.ControllerInfo` is final with a package-private constructor — nothing outside Media3 can build one. So the policy is proven and the *reading of the inputs* is not. | The failure it permits is precise: a wrong field read there admits or refuses the wrong callers while every test stays green. It is R-43's shape, narrowed from a whole feature to six field reads. | A device check — connect Android Auto and confirm the tree appears, connect an unrelated media app and confirm it does not. Media3 does expose `createTestOnlyControllerInfo`, which would close this under Robolectric, but it is `@VisibleForTesting(otherwise = PRIVATE)` and this project runs Lint with `checkTestSources` and warnings-as-errors, so using it needs a suppression decision rather than an import. |
| R-61 | ~~**The zero-duration fallback switches coordinate spaces and overwrites stored progress.**~~ **Closed 2026-08-27.** `BookMediaSourceFactory` fell back to the plain factory when any track reported a duration of zero, so the player's timeline became the first file — while `MediaItems.queueFor` still handed it a whole-book `startPositionMs` and both progress writers still read `currentPosition` as a book offset. | Resuming a 34-hour book at 4 hours seeked past the end of a 20-minute file, and then every journal tick wrote that file-relative position into the book's stored progress **and sent it to the server**, where every other device reads it. Product priority 2 is *do not lose progress*. The factory's KDoc called the fallback "a real degradation and the honest one"; it was not honest, because nothing downstream was told. Reachability was and remains unproven — no capture has produced a zero duration (22.5) — so this was a latent path, not an observed failure. | **Two halves, and the dead end recorded first: passing `C.TIME_UNSET` as the placeholder and always concatenating fails at runtime** — `ConcatenatingMediaSource2.Builder.add` asserts *"Progressive media source must define an initial placeholder duration"* — so the premise that every duration is needed up front is correct. **(1) Recovery.** `TrackDurations.recovered` computes a single unknown track's length as `book total − sum of the known`, which `PlaybackSessionDurationTest` licenses by pinning a session's `duration` equal to the sum of its `audioTracks` on both play fixtures. It refuses rather than guesses when more than one track is unknown, when the remainder is not positive, or when **any track is excluded** — no capture has an excluded track, so whether the total counts one is unestablished (22.4), and a third test turns red when a capture finally settles it. **(2) Containment.** `MediaItems.isSingleFileFallback` is the factory's own fallback condition, exposed rather than duplicated, so `queueFor` starts such a book at zero and both writers decline. The session plays and records nothing, which the factory's warning names. Reverting either half turns a named test red — verified by mutation, both ways. **Residual, and it is R-43's shape again:** the two writer gates (`PlaybackService.positionSnapshot`, `SessionSyncCoordinator.snapshot`) are not reachable from a JVM test, so the predicate is proven and the *calling* of it is not. A device check is the only way to close that, and the path needs a server that reports a zero duration, which none has. |
| R-62 | **Closed 2026-08-24 — ADR-0026 decision 3.** The app-switcher thumbnail showed the library — book titles, private self-hosted data under 14.5 — to anyone who picked the phone up, with no passcode prompt in the way. `docs/gaps.md` had it recorded as checked rather than assumed: nothing in the tree suppressed it on any API level. | The lock curtain guards *entering* the app and did nothing about the picture Android had already taken of it. | `setRecentsScreenshotEnabled(false)` from API 33, via `RecentsPrivacy.canSuppressThumbnail`. **Not `FLAG_SECURE`**, which the owner declined and which would block every deliberate screenshot and screen recording for every user to solve a narrow problem for some. **API 26–32 keep the thumbnail** — that is the accepted residual, not an oversight: the only lever there is the blunt one, and ADR-0026 records where a stronger option belongs if wanted (tied to a locked profile, or a privacy setting). A test asserts the gap so closing it later is a decision rather than a changed comparison. |
| R-63 | **Closed 2026-08-24. Narrowing session commands left the *player* commands wide open, and an untrusted controller could still clear the queue and stop the book.** R-59's fix called `setAvailableSessionCommands` and nothing else; `Player.Commands` is a separate set with its own default, and Media3's default grants `COMMAND_SET_MEDIA_ITEM`, `COMMAND_CHANGE_MEDIA_ITEMS`, `COMMAND_STOP` and `COMMAND_RELEASE` to everything that connects. `onSetMediaItems` compounded it: an id that resolved to nothing returned `emptyList()`, which Media3 applies, so an unresolvable request wiped whatever was playing. | **Worse than the leak the policy was written for.** The browse gate held — verified from a second UID on an SM-S928B: browse and library commands denied, custom commands absent, the attacker's URI never loaded and no bearer sent — and playback stopped anyway. Product priority 1 is *do not interrupt playback*, and this was reachable by any installed app. | Found by the 2026-08-24 device session, not by the suite, and that is the finding about the suite: `ControllerTrustTest` had ten passing tests over a policy that was only half-applied. `withheldPlayerCommands` now carries the four, `onConnect` calls `setAvailablePlayerCommands`, and an unresolved request returns the currently loaded item instead of an empty queue. Reverting the withheld list turns `an untrusted controller may not change the queue or stop playback` red; a companion test pins that play, pause and every seek survive, because withholding one of those would turn a privacy fix into a broken Bluetooth remote. |
| R-64 | ~~**The car's browse root rendered zero items on a real head unit while search returned books.**~~ **Closed 2026-08-27 — the cause is R-66, and it is not what this row guessed.** The hypothesis here was a cold-start profile race caching an empty root. The 2026-08-27 session refuted it with one observation: **warm start was identically empty**, and the log carried `A browse request failed error=unknown` twice with no `children=` line at all — so the browse was *throwing*, not answering empty. See R-66 for the actual defect (a `Player` property read from a background thread). | A car that connects and shows nothing is indistinguishable, from outside, from a refused node or a cached empty answer — which is why this row guessed, and why the `children=` diagnostic was added before guessing again. It earned its keep by being **absent**. | The diagnostic did its job and the hypothesis was wrong; both are worth leaving on the record. **The lesson is the one PRODUCT_SPEC 22.4 states for server behaviour, applied to this app's own:** a hypothesis that fits every observation is still not evidence, and this one fitted every observation and was wrong. |
| R-66 | **Closed 2026-08-27. The car's browse tree read the player from a background thread, so every browse threw.** `onGetChildren` and `onGetItem` called `nowPlaying()` *inside* `future`, whose block runs on `applicationScope` — `Dispatchers.Default`. `nowPlaying()` reads `player.currentMediaItem` and `player.currentPosition`, and ExoPlayer throws `IllegalStateException` for any property read off its application thread. **This is R-64's real cause**, and it retires the hypothesis R-64 carried: not a cold-start profile race, and not the trust gate. | A head unit showed an empty browse tree while everything else worked. The 2026-08-27 session pinned it: **two** `A browse request failed error=unknown` lines — the two roots Android Auto asks for on connect — no `A browser asked for a node's children` line at all, and **search returning the whole library**, because `onGetSearchResult` is the one browse callback that does not need the player. Cold and warm behaved identically, which is what refuted the race hypothesis. Three device sessions were spent on this. | `nowPlaying()` is now read in the callback itself, which Media3 invokes on the application thread, and the value is passed into the coroutine as a plain number. Its KDoc had said to do exactly that since it was written — *"The browse tree then uses it off-thread as a plain number, which is safe precisely because it was copied out here"* — so this is **R-32 in the harder direction**: the documentation was right and the code was wrong, and reading either alone looks correct. **Residual:** no JVM test can construct a `MediaSession.ControllerInfo`, so the session callbacks are unreachable from the test tier and the *calling* discipline is unverified by tests (R-43's shape, as with R-60). §2 of `docs/device-test-0.9.14.md` is the verification: a `children=` line for each root is now the pass condition. |
| R-67 | **Closed 2026-08-27. Choosing a language crashed the app, and kept crashing it on every launch.** `AppLocale` provided `context.createConfigurationContext(...)` as `LocalContext`. That returns a **`ContextImpl`**, which is not a `ContextWrapper`, and `androidx.hilt.navigation.HiltViewModelFactory` finds the Activity by walking `ContextWrapper.baseContext` — so the loop could not take one step and threw `IllegalStateException: Expected an activity context for creating a HiltViewModelFactory but instead found: android.app.ContextImpl` at the first `hiltViewModel()` inside the wrapper. | **Launch-blocking and self-perpetuating.** The choice is persisted, so every subsequent start rebuilt the same broken context before anything could be tapped, and changing the language in Android's own settings did not help because the app reads its own stored value. Recovery required uninstalling and reinstalling. Found on an Android 16 device on 2026-08-27; `AppLanguage` has shipped since 2026-08-20. | `LocalizedContext`, a `ContextWrapper` whose base is the Activity and whose only override is `getResources()` returning locale-resolved resources — which is the seam Compose 1.8.3's `stringResource` actually reads (there is no `LocalResources` in that version; verified against the artifact). Everything else delegates to the Activity. **Why four render tests missed it:** `AppLocaleScreenTest` asserted that the words change and never called `hiltViewModel()`, so Hilt's loop never ran — R-43 again. Three tests now assert Hilt's own loop, copied from its bytecode, and reverting the fix turns six of them red. |
| R-68 | **Reopened and closed again 2026-08-30, by a different fix. The 2026-08-27 one was half right and created a second way to break the same thing.** That fix made the four *release* signing inputs sign the debug build too, so a stable key made `adb install -r` an upgrade. What it also made was a debug signature that depended on **whether four environment variables were set in the shell that ran Gradle** — a release build in one terminal and a debug build in another produced two differently-signed debug APKs. The owner reported still having to uninstall before every debug build. | The original cost, unchanged: an uninstall wipes the sign-in, the passcode, the progress journal and every downloaded book, on the one activity that finds defects. What the first fix added was that the cause was now *invisible* — nothing on screen said which key had been used, and the trigger was an environment variable rather than anything in the checkout. | `DebugSigning.kt` gives the debug build one key of its own that nothing else can change, at `~/.bookwave/debug.keystore`. **It adopts `~/.android/debug.keystore` when one exists**, so the change is free for anybody with the app already installed — verified: the adopted keystore and the APK built from it both report `aa5fd8f7…`, matching the file copied from. On a machine with none it generates one with Android's public debug credentials. Release inputs no longer touch the debug build at all. CI shares one key through `BOOKWAVE_DEBUG_KEYSTORE_BASE64`; without that secret a runner still generates a fresh key per run, which the workflow now says in as many words. `bookwave.signing.debug.stable=false` opts out. |
| R-69 | **Closed 2026-08-27. An unsigned release APK was silent about being unsigned.** `assembleRelease` with no key configured succeeded, wrote an `.apk`, and said nothing; the failure surfaced minutes later on a phone as an install error naming the package rather than the signature. | A device session reported *"the release apk is not installable"* as a mystery. The build had known the answer at the moment it produced the artefact and did not say it, which is the same class of defect as R-64's `error=unknown`: the information existed and was discarded at the one place it would have been read. | `assembleRelease` now warns, naming the four properties and pointing at the keytool command in `docs/release.md`. Attached to the task rather than logged at configuration time, because configuration runs on every Gradle invocation and a warning that prints when it does not apply is one people learn to scroll past. The message is built line by line rather than as one interpolated `trimIndent` literal — the first attempt spliced a multi-line value into one and `trimIndent` dragged the common indent to zero, which was observed in the output and fixed rather than shipped. |
| R-71 | **Open. A car browses, and a tapped book never opens.** The 2026-08-28 head-unit run found the browse tree populated — `parent=root children=6`, and three tabs behind it — while tapping a book stayed on *"Henter valget ditt"* indefinitely. **Not diagnosed, and two hypotheses were refuted rather than confirmed.** (a) *A throw in `onSetMediaItems`*, the R-66 shape: refuted, because `future`'s failure path logs `A browse request failed` for every session callback and the run's log carried none. (b) *A withheld `COMMAND_SET_MEDIA_ITEM` from PR #48*: refuted, because `mayBrowse` and `withheldPlayerCommands` both read `ControllerTrust.accessFor`, so a controller that browsed held `LibraryAndPlayback` and every player command. | A car that lists a library it will not play is worse than one that lists nothing: it looks like it works. This is the surface PRODUCT_SPEC 11.1 exists for and the one a driver cannot work around. | **The reason it could not be diagnosed is the fix.** A car tap that *resolved* logged nothing at all — `onSetMediaItems` had no logger call, `openQueue` logged only on failure, and `onPlaybackStateChanged` logged nothing — so a queue that was set and never prepared and a queue that was never set produced identical logs. Two lines now separate them: `A controller asked to set what plays` (which callback, the id's *shape*, how many asked, how many handed back, the start position) and `The player changed state`. **`resolved=false` means resolution failed** — read that, not the count: when nothing resolves and a book is already playing the service hands that book back to keep it alive, so `handedBack=1` appears for a request that resolved nothing. `resolved=true` with no `state=buffering` is the player never taking it. `scripts/device-test/02-car-selection.sh` and `.ps1` capture exactly that window: clear the buffer, tap, dump. **A standing limitation of those scripts, and the reason they took nine review rounds:** they attribute a defect to a *layer* from the presence or absence of log lines, and log evidence supports that only under conditions the script has to establish first — that logcat is carrying the app at all, that the window was actually isolated, and that a transition belongs to the tap rather than to a book that was already streaming. Every one of those was a defect before it was a guard. They are guards now, and each is checked in one place rather than at each call site, but the class is inherent to diagnosing from greps: the scripts report evidence and refuse to locate a layer when the evidence cannot carry it. **Their own first real test is the next device run**, which is also the run that settles this row. One more DHU run settles it, which is the point — R-64 cost three sessions to guessing. |
| R-72 | **Closed 2026-08-28. R-66 had a third site, and it was the one a car reaches when a tap does not resolve.** `unresolved()` read `player.currentMediaItem` and `player.currentPosition` from inside `onSetMediaItems`'s `future { }` block — `Dispatchers.Default` — and ExoPlayer throws `IllegalStateException` for any property read off its application thread. R-66 fixed `onGetChildren` and `onGetItem`, the two the device run had exercised, and left this one. | Reached whenever a controller names an id that no longer resolves. Instead of ROUTE-001's *"the command does nothing and logs a non-fatal diagnostic"*, the future completed exceptionally and a head unit waited on a spinner forever. **Not** the cause of R-71 — the missing `A browse request failed` rules it out — but the same defect one step along. | **The first fix was a snapshot on the callback thread, and it was wrong** — a review caught it before it shipped. `openSession` sits between the snapshot and its use, it is a network call that can fail slowly, and the book keeps playing meanwhile; handing Media3 a position captured seconds earlier makes it seek there, rewinding a book nobody asked to move. That is product priority 2 broken by the fallback whose whole purpose is priority 1. `unresolved` is now `suspend` and reads the player *at the moment of use*, hopped onto its own thread with `withContext(mainDispatcher)` — fresh **and** on the right thread, which a snapshot could only ever be one of. **The KDoc that stood over `unresolved` is the lesson, and it was wrong twice in the same way:** it said to read once into a local *because two reads of a mutable player can disagree* — correct, and silent both about which thread was reading and about how stale the answer could be. R-32 in the harder direction: documentation that is right about one hazard reads as right about all of them. The residual R-66 recorded still stands — no JVM test can construct a `MediaSession.ControllerInfo`, so no test reaches any of the three call sites. |
| R-70 | **Closed 2026-08-28. `adb logcat` carried nothing from the app, and the scripts read that as a finding.** On an SM-S928B every `logcat -d` grep in `scripts/device-test/` came back empty while the in-app event log held all four `children=` lines. The scripts dumped the buffer long after the step being measured, and on that device it had rolled. | **This is R-15's shape and it nearly cost a correct result.** §2's pass condition was "a `children=` line for each node", the tester ran the documented command, got nothing, and only the in-app log showed that the browse had in fact worked perfectly. A signal that returns empty whether the app succeeded or failed teaches people to ignore it. | Two changes, because one would not have been enough. `logcat_clear` / `Clear-Logcat` empties the buffer *before* the step, so the window is small and fresh. And every grep now runs a preflight counting the app's own `ShelfPlayer` tag: zero lines means **the measurement is broken**, and the script says so and points at the in-app log, rather than presenting an empty result as an absence. Both branches were exercised in both shells against a stub `adb` — one carrying the app's lines, one carrying only another app's — and the two produce visibly different output. |
| R-73 | **Closed 2026-08-28. The same `Join-Path` null crash, in a second file, three weeks after the first.** `_common.ps1`'s SDK search composed `(Join-Path $env:LOCALAPPDATA 'Android\Sdk')` unconditionally inside a list literal. `Join-Path` throws on a null `Path` rather than returning null, so every PowerShell device script died on its first line wherever that variable was unset. | Not reached on the tester's Windows box, where `LOCALAPPDATA` is always set — so it shipped in PR #57 having passed a real hardware run. It makes the scripts unrunnable anywhere else and on any trimmed Windows environment, and it is a crash *before* any diagnostic prints. | Candidates are built only from variables that are set, the same fix already applied to `Set-BookWavePath.ps1`. **The lesson is about the method, not the bug:** the first instance was found by *executing* the script in a `mcr.microsoft.com/powershell` container, the second by executing this one; neither was found by reading, and both files had been read carefully. A parse check passes on both. Every `.ps1` in `scripts/` now parses and `02-car-selection.ps1` executes end to end under a stub `adb`. |
| R-74 | **Open, and narrowed 2026-08-30. §0.2 is recorded PASS but cannot detect what it is for**, and the setup it prescribed has since been deleted. The step asks for a second `:app:installDebug` over the first; on one machine that passes whether or not any fix is present, because the machine that builds keeps its keystore. It also used to tell the tester to answer **No** to `06-create-signing-key.ps1`'s *“Also sign debug builds with this upload key?”* — advice that made the step un-runnable, since without a shared key two machines cannot produce the same signature. | R-68 was recorded closed twice on the strength of a test that could not have detected either failure, and the owner found the second one by using the app. | The prompt is gone: debug signing no longer has anything to do with the upload key, so there is nothing to decline. What still needs running is the case that was always the point — **a CI-built APK installing over a locally-built one, both directions**, which now requires `BOOKWAVE_DEBUG_KEYSTORE_BASE64` to be set. Until that runs, the claim that CI-to-local upgrades work is untested. |
| R-75 | **Closed 2026-08-29. Every Android Auto result this project holds came from the Desktop Head Unit, and the DHU is not a car.** It is the same projection stack driving a window on a computer, so it covers the browse tree, search, selection and transport — and covers none of the car's own contribution. Both report `controller=com.google.android.projection.gearhead`, so **the log cannot tell a DHU run from a car run**; only the tester can. | Six things can fail for a listener while every recorded test stays green: the app missing from the **car's own launcher** (a discovery defect the DHU's launcher cannot show), the **steering-wheel and hard buttons** (media-button events that never touch the DHU, and PR #48 narrowed exactly that command surface), **driving restrictions** truncating a list into uselessness, an **ignition cycle** (closing a DHU window is not a power down), an **unplugged cable** mid-book — product priorities 1 and 2 in one step — and **voice**, which enters `onSetMediaItems` through the search-query branch rather than a media id. Automotive OS is a third host again, `isAutomotiveController`, and no test here has ever reached one. | §2.9 and `scripts/device-test/02-real-car.{sh,ps1}` are those eight steps, written to be run parked, with the one that needs the car moving marked as needing a second driver and skippable — an untested restriction is a better outcome than an accident. §2.0 states the host distinction up front and asks the tester to record which one they used, because the log will not. Retiring this needs a car and about half an hour. **Closed by the owner's car run on 2026-08-29**, which reported the section passing as a whole. What that leaves: the eight steps were not itemised, so the launcher, the wheel, the driving restrictions, the ignition cycle, the unplug and voice are recorded as passing on the tester's word rather than from eight written answers, and Automotive OS is still a host this app has never met. |
| R-76 | **Closed 2026-08-29 before it ever ran. The steering-wheel step required a Media3 reason that this service can never log.** §2.9's wheel check passed only on `reason=remote`, on the reasoning that `userRequest` is the phone's own UI. That is wrong about the library: a controller's play/pause arrives at `MediaSession`, which forwards it to the local player as `play()` → `setPlayWhenReady(true)`, and `ExoPlayerImpl.setPlayWhenReady(boolean)` hard-codes `PLAY_WHEN_READY_CHANGE_REASON_USER_REQUEST` (verified in the 1.7.1 bytecode: the call pushes `iconst_1`, and the only other reason that class emits is `3`, becoming-noisy, from its `ComponentListener`). `REMOTE` is what a *remote* player reports — `CastPlayer`, or a controller observing a session — and `PlaybackService` listens on the local ExoPlayer (`PlaybackService.kt:201`). | The verdict could not pass on any hardware. A working steering wheel would have been recorded as an unsupported or refused one, on the first real-car run this project has ever done, for the command surface PR #48 narrowed — so the wrong answer would have arrived exactly where there was no prior result to contradict it. It was introduced by the fix for the opposite defect (a dead wheel passing on the previous step's lines), which is the third time on this branch that a fix for an over-claim has produced an under-claim. | The pass is now a play/pause line in the cleared window, with the tester told not to touch the phone — because the reason genuinely cannot name the presser, and isolation is the only attribution available. Both shells and §2.9 say so rather than implying the log knows. The general lesson is R-71's: state what the evidence carries. The specific one is narrower and worth keeping separate — **a constant's name is not its contract; check which call site emits it.** |
| R-77 | **Open, and narrowed 2026-08-29 by a device run and by an API this project had wrongly ruled out.** `ExoPlayer.setPreferredAudioDevice` is a *preference*: the platform honours it while the device is connected and available, and silently declines it otherwise — `AudioTrack.setPreferredDevice` returns a boolean and Media3's wrapper swallows it. The owner hit the decline in the case it is most likely: **choosing the phone speaker while a headset is connected changed nothing**, and the app showed the speaker as chosen. What was *also* wrong was ADR-0027's premise that no API can say where media is going. `AudioManager.getAudioDevicesForAttributes` has been public since **API 33**. | Before the fix a listener could choose an output, see it ticked, and hear no change, with the app confidently reporting the selection — indistinguishable from success without ears. The remaining risk is the decline itself, which no app can prevent: there is no API that forces a media route, and the system output switcher cannot be driven programmatically. **Below API 33 the old blindness stands**: minSdk is 26, so Android 8 through 12 still have nothing to read and the app falls back to reporting the request. | `AudioOutputRouter` now reads the live route for `USAGE_MEDIA` on API 33+ and publishes it as `AudioOutput.isActive`, separately from the chosen id. The menu ticks the choice and labels the route *“playing here”*, so a declined request is visible as a disagreement between the two rather than as a tick that lies; the same label marks the row in the car. The `apply` log line carries `asked`, `routedTo` and `honoured` (kinds only — 14.5), so the decline is diagnosable from the event log. Fully retiring this needs a platform that lets an app command a route, which does not exist. |
| R-78 | **Closed 2026-08-29 — the button was buildable and this row said it was not.** The row claimed the Android Auto player could have no output button *“because no API allows one”*. What no API allows is a custom action **opening a browse node**; a custom action itself has always been available, and this app already ships three of them. The inference from one to the other was never checked, and the owner found it by looking at the car: *“there is still no button”*. | The cost was a whole feature not built for a day, on a stated impossibility that a reader of this register — including me — had no reason to re-examine. This is R-32's failure mode with a platform claim instead of user-facing prose: **a written-down impossibility is the thing most likely to go unchallenged.** | Closed by building it. The player now carries a Bluetooth-icon button that steps to the next output (`AudioOutputCycle`, ADR-0027's second amendment), verified against the Media3 bytecode first, in 1.7.1 and again in 1.11.0: the legacy custom action is built from `CommandButton.iconResId`, and `getCustomLayoutFromMediaButtonPreferences` requires `SLOT_OVERFLOW`. (In 1.11.0 the first of those moved from `PlayerWrapper` to `MediaSessionLegacyStub` — the behaviour held, the location did not.) **What remains true and is now R-81**: the button cannot open a *list*, so it steps, and the full list stays a browse tab. |
| R-81 | **The car's button steps rather than choosing, and a driver cannot see what the next press will select.** No API opens a browse node from a custom action and the Android Auto player template has no submenu, so a button can only send a command. The order is `[Automatic] + connected outputs`. | A driver wanting a specific output may press past it, and each press moves the audio on the way. With the two or three outputs a car actually has this is quick and reversible; with more it is a poor way to reach the last one. The button carries its current destination in its name and icon, which is what makes the stepping legible rather than blind. | The **browse tab** is the mitigation and is not redundant with the button: it shows every destination at once, marks the route and the choice separately, and is one swipe from the player. Retiring this needs Android Auto to gain either a submenu on the player or a navigate-to-node command; neither exists. Accepted deliberately over the alternative the register used to prescribe, which was no button at all (R-78). |
| R-82 | **A book that ends while nobody is listening now starts the next one.** PRODUCT_SPEC 6.4 step 6 asks for it and the owner asked for it; it is on by default, because the alternative — a series read in order stopping dead between its parts — is the stranger behaviour for somebody who *is* listening. | Somebody who falls asleep near the end of book 3 wakes to book 4 several hours in, holding progress on a book they have not heard. Nothing is lost — book 3's finished state was journalled before the advance, which is why the advance is launched after `recordPosition` rather than beside it — but a stored position on book 4 is a *wrong* one, and the fix is manual. It also spends data or battery on somebody who had stopped. | **PLAY-008's sleep timer**, which is the control that already answers "is anybody still awake" and stops the book wherever it is; the settings hint says so in as many words rather than assuming the connection is obvious. And the switch itself: `autoAdvanceSeries`, stored inverted so proto3's zero value is the default rather than a silent opt-out. Not fixable beyond that — an app cannot tell a sleeping listener from a still one, and guessing would be a worse failure in the other direction. |
| R-83 | **The toolchain is pinned by two ceilings that meet in the middle, and almost nothing can move between them.** Measured 2026-08-30 by running the upgrades rather than reading release notes. **Above**: AGP 9.1+ is *required* by current AndroidX — core 1.19, lifecycle 2.11, navigation 2.10, hilt-navigation-compose 1.4 — and AGP 9.0+ by Hilt 2.59+. **Below**: detekt 1.23.8 loses type resolution against a Kotlin stdlib newer than 2.2.x, so Kotlin 2.2.21, coroutines 1.11 and kotlinx-serialization 1.11 (which forces `kotlin-stdlib 2.3.20`) each turn its analysis into false positives — `RedundantSuspendModifier` on functions that call `Flow.first()`, `UnreachableCode` on ordinary `return` statements. detekt 1.23.8 is the newest on its line. | The project can take Hilt 2.58 and KSP 2.3.11 and nothing else in the Kotlin toolchain. Kotlin is stuck at 2.2.0 — not because 2.2.21 breaks the build, but because a **linter** cannot analyse it, and the only ways past that are suppressing a correctness rule at every site it misfires (which spread to 14 findings across `core:network` when tried) or dropping detekt. Both are worse than waiting. | **AGP 9 unlocks the top half and detekt 2.x unlocks the bottom half**, and until one of them lands the toolchain is where it is. `docs/dependency-upgrade-plan.md` carries the measured evidence. Nothing here is a defect in this app; recorded so the next person does not re-derive it, and so that a green `verifyDebug` on an older toolchain is not mistaken for the toolchain being current. |
| R-84 | **The metadata-only half of `onPlaybackResumption` cannot be reached, so nothing on a device has ever run it.** Media3 1.11.0 added `isForPlayback`; the `false` case is System UI asking, at boot, for enough metadata to draw a resumption notification. `PlaybackService` answers it with the resume tile and opens no server session — but Media3 gates that whole path on `MediaSessionLegacyStub.canResumePlaybackOnStart()`, which is `broadcastReceiverComponentName != null`: a `MediaButtonReceiver` declared in the manifest. This app declares none (its receiver is the service), so the branch is dead code today and only `AutoLibrary.resumeItem` is covered by a test. | Dead code that looks live is how a wrong answer survives review. The specific hazard is the opposite of the usual one: the branch is *safer* than the code it replaced, so nobody will notice it is not running, and the day somebody adds a media button receiver to the manifest for an unrelated reason it will start answering a question this project has never observed being asked. | Recorded rather than fixed, because the fix is to add a manifest receiver the product does not need. **If a `MediaButtonReceiver` is ever added, this branch becomes live and needs a device check**: reboot the phone with a part-played book and confirm the system's resumption notification shows that book, and that the server records no listening session until the notification is actually pressed. |
| R-79 | **`ShelfPlayerDatabaseTest` flakes on a loaded machine, and the failure looks like a defect.** `progress is scoped to its profile` is a Robolectric + Room test that completes in a couple of seconds alone. On 2026-08-29 it failed the gate with `UncompletedCoroutinesError: After waiting for 1m, the test body did not run to completion` after **97 seconds** of wall clock, while a Gradle build and a container were competing for the same cores. It passed in 28 s on a re-run with nothing else going. | The message names a coroutine that never completed, which reads as a deadlock rather than as a slow machine — so the honest response is to go looking for one. Any test using `runTest`'s default one-minute timeout on a Robolectric database has the same shape, and CI runners are shared. | Not fixed, because the fix is a judgement about which number to pick and this build has seen it once. If it recurs, the answer is an explicit `timeout` on that `runTest` sized to the work rather than to the default. Recorded so the next person to see it re-runs before investigating. |
| R-80 | **Accepted 2026-08-29 — ADR-0028. An unlocked phone with an admin profile active is full administrative authority over the server, as far as this app is concerned.** No privileged operation re-prompts: removing an item from the server database, creating a user, editing metadata and embedding metadata into files all run on the strength of the session. The owner was asked and decided that being signed in as admin is enough. | Somebody who picks up the unlocked phone can do all four. The one that cannot be undone is the embed, which rewrites audio files in place; the database removal is the one whose blast radius is largest. This is the same threat ADR-0023 named for the profile lock — *"somebody holding the unlocked phone"* — now accepted for the management tools too. | Not a defect and not to be "fixed" quietly: it is a recorded decision, and reversing it means step-up authentication before the four operations, which is a change to make deliberately. What still applies: the **server's own permissions**, because every one of these is a server call carrying the bearer token and an account without admin rights cannot perform them however this app behaves; the confirmation each destructive action already shows, which guards a mis-tap rather than an attacker; and the device lock screen, which is what actually governs who is holding the phone. Revisit if a shared-device story ever appears. |
| R-58 | **The scroll number the harness produces is a floor, not the whole answer.** `BenchmarkLibrarySeeder` writes 2,000 books with `coverPath = null`, because a cover is fetched from the server the book came from and the fixture has no server. So frame timing over `BooksView.List` excludes image decode, which on a real library is a substantial part of what scrolling costs. | A green scroll result does not prove a real 2,000-book library scrolls well — it proves the list and the un-paged `Flow<List<Book>>` underneath it are not the bottleneck. Read as a regression baseline against itself it is sound; read as "scrolling is fine" it overstates. | Either seeding local cover files into the fixture, or one manual scroll pass against the owner's own library with covers loaded, recorded next to the automated figure. The second is cheaper and answers the question that matters. |
| R-40 | **The passcode is a six-digit secret behind a KDF, and that is all it is.** A million possibilities at 210,000 PBKDF2-HMAC-SHA256 iterations is roughly 2 × 10^11 hash iterations, which a single modern GPU exhausts in minutes. The verifier does not resist an attacker who is holding the file. | Nothing against the threat ADR-0023 actually names — somebody holding the unlocked phone, who has to go through the app and meets a rate limit stored inside the encrypted record where force-stopping cannot reset it. Everything, the moment this lock is described as protecting data at rest, or a later feature treats the verifier as if it were a key. | Nothing available, and that is the point of recording it. Raising the iteration count roughly triples a number that already loses, while making an old phone take seconds to reject a typo — and a lock that slow gets switched off. The mitigation is the wording: ADR-0023 and the curtain both say what this is, and neither may be quietly softened. |
| R-41 | **Closed 2026-08-21.** The relock delay fired for nobody: `ProfileLockGate` stamped every live ticket when the app left the foreground and compared that stamp to the clock on each read, and the two functions supplying the stamp had **no production caller**. `backgroundedAt` stayed null for the life of the process, so `isUnlocked` returned true unconditionally after one unlock and the setting's three values — `Immediately` included, which is the default — were indistinguishable. | A profile unlocked once stayed unlocked until Android killed the app, so the curtain guarded a cold start and nothing else. Somebody who turned the lock on expecting it to re-arm when they pocketed the phone had exactly the exposure R-19 used to describe, with a settings row telling them otherwise. | `ProcessLockWatcher` calls both functions from `Application.ActivityLifecycleCallbacks`, and `ProcessLockWatcherTest` fails if it is removed. It counts started activities and ignores `isChangingConfigurations`, so a rotation is not treated as leaving the app — without that, the default delay would demand a passcode every time the phone turned sideways. Whether `Immediately` is the right *default* is still open and does need a device. |
| R-20 | **`allowBackup="false"`**, so a device-to-device transfer carries nothing — not the sign-in, not the settings, not the downloads. | Deliberate, and the right default for a store of server credentials. It is a risk only in that a user replacing a phone will read it as data loss unless something says so. | A line in the release notes. Not a code change. **Reaffirmed 2026-08-27.** Backing up the server URL and username so they survive a reinstall was raised — the motivation being a device tester retyping them after every build — and the owner declined it, along with ever backing up the token. The retyping had a different cause (R-68, ephemeral debug signing) and that cause is now fixed, so the convenience this would have bought no longer needs buying. If it is ever reopened, the token stays out regardless: that is a separate and standing decision. |
| R-21 | **Progress is the only thing this app can lose that the user cannot recreate.** Everything else re-syncs. | Product priority 2. The outbox and the session coordinator exist for this, and R-11 is the reason the guarantee is unmeasured. | R-11. |

---

## 5. Playback correctness

| # | Risk | If it bites | Retired by |
| --- | --- | --- | --- |
| R-22 | **Closed 2026-08-24 — the defect described here cannot occur, and the mitigation this row used to recommend would have been harmful.** The row claimed a book with an excluded file resolves positions against the wrong offsets, and prescribed carrying `startOffset` into the media item's extras "as ADR-0016 describes". ADR-0016 says the opposite: it *deletes* that key and the two-coordinate-space conversion with it. `docs/gaps.md` had already closed the same item on 2026-08-20 as *not a defect*; this row and `docs/closeout.md` were not updated, so a closeout written on 2026-08-24 inherited it and put it second in the work order. | Audiobookshelf's `Book.getTracklist` filters `includedAudioFiles` **before** it accumulates `startOffset` (its source is quoted verbatim at `CapturedShapesTest.kt:514`), so `media.tracks` never contains an excluded file and the offsets never contain a hole. `PlaybackSession.playableTracks` filters again upstream, and `isExcluded` appears nowhere in `playback/` — `MediaItems.Track` has no such field, so the playback module structurally cannot mishandle exclusion. `startOffset` is written by three mappers and read for arithmetic by nothing. | Nothing to fix. What this cost is the lesson: **R-32 is not only about user-facing prose.** A stale risk row sent the work order to a phantom, and the only reason it was caught is that the fix was investigated before it was written. |
| R-23 | **A dropped websocket leaves an embed's outcome unknown.** Nothing replays a missed `task_finished`. | The app says "outcome unknown — verify on the server", which is honest and is also the worst possible answer about an operation that rewrote audio files. | Polling the task endpoint after a reconnect, if one exists. Not investigated. |
| R-24 | **Auto-play from a cold start is unreliable** — Android's background-start rules, not this app's. The setting says so. | A user who configured auto-play on a car connection sees it work sometimes. | Nothing. Correctly disclosed rather than fixed. |

---

## 6. Performance, never measured

| # | Risk | If it bites | Retired by |
| --- | --- | --- | --- |
| R-25 | **The macrobenchmark harness is built; the numbers are not taken yet.** `:benchmark` measures cold start (time to initial *and* full display), frame timing while scrolling `BooksView.List`, and Home's peak memory — all over a seeded 2,000-book library — and `BaselineProfileGenerator` records the profile. What remains is running it: every task needs a device and CI has no emulator. Two of PRODUCT_SPEC 17.3's four numbers stay manual by nature — player start from a cached book and no-ANR-under-stress both need a real server and a real download, and a committed benchmark pointed at somebody's private instance would be unreproducible and would put a host name in the repository (14.5). `docs/benchmark.md` is the runbook and holds the empty results table. | Startup and scroll are the two things a user judges in the first ten seconds, and a harness nobody has run is not evidence about either. A baseline profile is worth roughly 20–30% of cold start and the *consuming* half is already free — `androidx.profileinstaller` is on the release classpath transitively, so shipping `baseline-prof.txt` needs no new dependency, only the file. | `./gradlew :benchmark:connectedBenchmarkAndroidTest` with a phone attached, then filling in `docs/benchmark.md`'s table and committing the recorded profile. **Partly retired:** the module, the fixture and the profile generator exist and are compiled by `verifyDebug` on every pull request, so the half that could silently rot does not. |
| R-26 | **Closed 2026-08-21 — ADR-0025.** The question was whether 17.3's *"scrolling grid ... on 2,000-item fixture library"* describes a screen this app has. **It does not.** A full sweep found `LazyVerticalGrid`, `LazyHorizontalGrid` and `GridCells` appearing zero times in the repository; Home is a `LazyColumn` of capped 20-item `LazyRow` shelves, the flat "all books" view is a list, and the library-browse destination was deliberately removed. | Building the benchmark first would have meant inventing a grid to satisfy a measurement, or measuring something else and calling it the target — the ADR-0016 failure again, where a "known defect" stood for four phases on one unchecked premise. | The target now measures `BooksView.List`, the screen that exists. ADR-0025 also adds the target 17.3 could not have named, because it describes a grid rather than an architecture: **there is no paging**, `Flow<List<Book>>` materialises every visible book per emission, and `HomeViewModel` reasons about that cost at 490 books — 2,000 is four times what the code was thought about at. Whether to adopt paging is deliberately left to the measurement. |
| R-27 | **Coil's memory cache is at its defaults** with covers loaded at shelf density. | Untested on a low-memory device, which is where an audiobook app spends its life — long sessions, screen off, background. | A measurement, not a change. Guessing at a cache size is how a cache gets worse. |

---

## 7. Accessibility and adaptive layout

| # | Risk | If it bites | Retired by |
| --- | --- | --- | --- |
| R-28 | **Narrowed: adaptive layouts exist, but wide hardware is not reviewed.** Book and player use two panes when width allows, sign-in is capped, and lists center rather than stretching. The 2026-08-23 review covered one portrait phone width only. | Tablet, foldable, split-screen, and landscape failures remain release-visible despite correct size-class branching. | One wide/tablet or resizable-window pass, one fold state, and landscape with Mini Player/insets visible. |
| R-29 | **Accessibility is enforced on the JVM; ordinary phone pixels were reviewed, TalkBack was not.** `assertEveryControlIsLabelled` and `assertEveryControlIsBigEnough` walk the semantics tree on Settings, book, downloads, profile switcher, player, mini player, and shelf, several at doubled font scale. The signed-in phone pass found a nested Genre Edit gesture failure, proving rendered reachability still matters. Label usefulness, contrast, TalkBack order, and 48dp platform dispatch remain out of reach. | TalkBack, large text, touch targets, and contrast are release requirements (§51, 2.10). The tree being correct is necessary and not sufficient. | One TalkBack/contrast/touch-target pass on a device and extending assertions to metadata plus every newly changed collection card. |
| R-35 | **The 48dp touch-target figure is asserted at 40dp.** `touchBoundsInRoot` — the rectangle the platform actually dispatches to, and the one that makes a Material `IconButton`'s 40dp state layer a 48dp target — is internal to Compose, and the public test API offers only equality assertions. So the check uses Material's smallest component size instead. | It still fails a 24dp icon used as a row's only control, which is the real failure mode. What it cannot prove is the last 8dp, which is a claim about Compose rather than about this app. | The device pass in R-29, or a public minimum-touch-target assertion in a future Compose release. |
| R-30 | **Large-text coverage is partial and RTL remains unexercised.** Several Robolectric screens render at doubled font scale, but the signed-in route inventory used the ordinary device scale and no RTL locale. | Long Norwegian labels at 200% and mirrored layouts can still clip or invert action order on routes outside the sampled tests. | Run the full changed-screen set at 2.0x and one RTL locale on a device or screenshot-test matrix. |

---

## 8. Process

| # | Risk | If it bites | Retired by |
| --- | --- | --- | --- |
| R-31 | **Local verification and CI can disagree.** Gradle considered test-compile tasks up to date when only the classpath had changed, so two stale test doubles compiled locally and failed in CI. | A green local build is not evidence. This cost a full debugging cycle once. | Already policy: verify with `--rerun-tasks`, and prefer a throwing property over an anonymous implementation of an interface that can grow. |
| R-32 | **Documentation drift has been the most frequent defect in this project.** The About tab described a build three phases old; `PRIVACY.md` and `SECURITY.md` still say "Phase 0: nothing leaves your device"; `docs/release.md` lists blockers that Phases 1–5 resolved. | Every one of these was user-visible or would have been read by somebody deciding whether to trust the app. | Treating user-facing prose as a deliverable of the change that invalidates it, not as a follow-up. |
| R-33 | **Twenty merged branches cannot be deleted from this session** (HTTP 403, and no MCP delete-branch tool). | Cosmetic. Recorded so it is not rediscovered. | A maintainer with push rights, or the GitHub UI. |
| R-34 | **Closed 2026-08-21.** There was no changelog and no label convention, both required by PRODUCT_SPEC 18 for a generated release note. | It blocked the release pipeline, cheaply. | `CHANGELOG.md` is written by hand and carries the reasoning behind each decision; `.github/release.yml` maps pull-request labels to sections so GitHub generates the note for a tag. The division is deliberate — the note indexes what merged, the changelog explains why. Unlabelled changes fall into a catch-all rather than being dropped, and nothing enforces a label, because a check that failed a pull request over one would block the fix for a labelling mistake. |
| R-37 | **A test double that does not reproduce the shape of the real thing hides defects behind a passing test.** PR #28 found that catalogue reconciliation deleted an entire library on any unchanged refresh: `LibrarySnapshot.books` carries only *expanded* items, an item the server reports unchanged is deliberately skipped, so a second refresh produced an empty list and called `markAllBooksDeleted` — and every read filters `isDeleted = 0`. A test named `refresh is idempotent and does not duplicate rows` had existed for months, refreshed twice, and passed. | It passed because `FakeAudiobookshelfGateway.listBooks` **ignored its `cached` argument** and returned every book on every call, so the fake's `books` was never empty and the production shape never occurred. The fake was not a double; it was a second implementation that agreed with nothing. A 60-second OkHttp `callTimeout` bounding the Media3 stream — PR #28's other serious find — escaped for the sibling reason: nothing in the suite plays for sixty-one seconds. | **The sync half is closed.** The fake now honours `cached.isUpToDate`, and the fixture books carry a server stamp — without one `isUpToDate` can never return true and the skip path is unreachable, which is why the blind spot existed. Reverting the production fix now fails `refresh is idempotent` through the full repository path. The playback half still needs R-07. |
| R-38 | **A defaulted parameter can preserve the bug it was added to fix.** `LibraryApi.listBooks` gained `onCatalogueBatch`, defaulting to `onBatch` so no implementer had to change. Any future persistence caller that does not pass a non-destructive sink silently gets the destructive behaviour the parameter exists to avoid. | Today there is exactly one persistence caller and it passes the sink. The trap is dormant, not absent, and it is the kind that reappears when a second caller is added by somebody reading the signature rather than the KDoc. | Removing the default once every caller is explicit, so the compiler asks the question instead of the reviewer. |

---

## What this register is not

It does not list requirements this build never claimed — those are in `docs/gaps.md`. It does not list
hypotheticals nobody has evidence for. And it does not list the things that look like risks and are not:
the empty capability set on a fresh server, the fake gateway's deliberate sign-in refusal, and the download
volume setting that moves no files are all in `gaps.md`'s final section with the reasoning.
